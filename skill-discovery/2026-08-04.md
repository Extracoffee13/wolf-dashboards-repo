# Skill / MCP / Platform Discovery Sweep — 2026-08-04

Sources checked: mcp.directory (403, blocked), modelcontextprotocol.io / blog.modelcontextprotocol.io, github.com (topics: `mcp-server`, `claude-skill`, `claude-skills`, `anthropic-skill`), npm (`mcp-server`, `anthropic-skill` keywords), pypi (`claude-skill`, `mcp` keywords), code.claude.com/docs (changelog + what's-new), anthropic.com/news (403, blocked — worked around via search).

**Method note:** GitHub topic pages sorted by "updated" are dominated by trivial commits/stars, not genuine new releases — they did not surface anything both *new* (last 24h) and *material*. mcp.directory and anthropic.com/news returned HTTP 403 to direct fetch; coverage there came from indirect search only. The one channel with a confirmed, dated, substantive release in the last 24 hours was Anthropic's own Claude Code changelog (v2.1.221, shipped 2026-08-04).

## Candidates (score ≥ 3)

### 1. Sandbox credential masking — Claude Code v2.1.221
- **Source:** https://code.claude.com/docs/en/changelog
- **Pitch:** New `mode: "mask"` option for credential files on Linux/WSL sandboxes — sandboxed commands read a sentinel copy of a credential while the real value is substituted only on egress, so secrets never sit in the sandboxed process's readable filesystem.
- **Score:** 4 — new capability the team would use weekly across every agent that runs sandboxed shell commands with API keys/tokens.
- **Suggested owner:** Sentinel

### 2. `prompt-audit` subcommand — `claude-api` skill, Claude Code v2.1.221
- **Source:** https://code.claude.com/docs/en/changelog
- **Pitch:** New subcommand in the built-in `claude-api` skill that scans prompts and tool descriptions for patterns written for older/stale models, flagging drift as new Claude versions ship.
- **Score:** 4 — directly useful for keeping the 18-agent C-Suite's system prompts current as Anthropic ships new model defaults (Opus 5 landed just 11 days prior).
- **Suggested owner:** Architect

### 3. Focus view — Claude Code v2.1.221
- **Source:** https://code.claude.com/docs/en/changelog
- **Pitch:** Chat-menu toggle (Ctrl+Alt+F) that collapses tool-call noise into expandable per-turn summaries with live running-tool indicators — makes long agent sessions readable at a glance.
- **Score:** 3 — niche but useful for anyone reviewing long-running WOLF/Praxis sessions.
- **Suggested owner:** WOLF

## Notable but out of window (not scored — flagged for awareness only)
- **Bumblebee** (Perplexity AI) — read-only supply-chain scanner that checks dependencies, MCP servers, and editor extensions for suspicious packages. Real release, but dates to May 2026, not the last 24h — worth a follow-up look given the growing MCP footprint, not included in the scored list above.
- **MCP spec 2026-07-28** ("stateless core" revision) — about a week old, not in window, but the ecosystem is still absorbing it; expect a wave of MCP server updates targeting it over the next few weeks.

## Bottom line
Thin cycle. No third-party skill or MCP server surfaced today that clears a 3+ score — today's only material, verifiably-dated releases came from Anthropic's own Claude Code changelog. Top candidate: **sandbox credential masking** (score 4, tied with prompt-audit).
