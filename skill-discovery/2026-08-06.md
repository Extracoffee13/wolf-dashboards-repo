# Skill Discovery Sweep — 2026-08-06

Sources scanned: mcp.directory / registry.modelcontextprotocol.io, GitHub (topics `mcp-server`, `claude-skill`, `anthropic-skill`, filtered to repos created in the last ~24-48h), npm (`anthropic-skill`, `mcp-server` keywords), PyPI (`claude-skill`, `mcp` keywords), claude.com/blog, anthropic.com/news.

No candidates scored a 5 as a true drop-in replacement for an existing skill; one scored close for the diligence/finance workflow. Broader platform context: Anthropic shipped the MCP 2026-07-28 spec (stateless core, MCP Tunnels research preview, Enterprise-Managed Authorization) in late July — not new today, but the spec churn is likely why several new servers this week lean on stateless/tunnel-friendly designs.

## Candidates (score ≥ 3)

### 1. `disclosures` (carrotly-ai) — Score: 5
- **Source:** https://github.com/carrotly-ai/disclosures
- **Pitch:** TypeScript library + MCP server over SEC EDGAR and GLEIF that surfaces filings, insiders, beneficial owners, financials, ownership chains, and private raises in one query — built-for-diligence data an agent can call directly instead of scraping.
- **Suggested owner:** Equity

### 2. `funnel-skill` (jessxdavid) — Score: 4
- **Source:** https://github.com/jessxdavid/funnel-skill
- **Pitch:** Plug-and-play Claude skill that generates a full call funnel — landing, booking, and confirmation pages plus email/SMS follow-ups — from a short conversation, aimed at agencies and coaches (i.e. Brand 9 Signs' own client segment).
- **Suggested owner:** Signal

### 3. `aimem` (yogesh-joshi-0333) — Score: 4
- **Source:** https://github.com/yogesh-joshi-0333/aimem
- **Pitch:** Local-first, project-scoped MCP memory server (SQLite + embeddings) giving coding agents persistent, queryable memory across sessions — same shape of problem the praxis-inbox pattern solves by hand today.
- **Suggested owner:** Architect

### 4. `amda-skill` (kubersharmax) — Score: 3
- **Source:** https://github.com/kubersharmax/amda-skill
- **Pitch:** Claude skill that classifies AI-generated marketing/GTM outputs into human-review governance zones — a lightweight QA gate for agent-authored copy before it ships.
- **Suggested owner:** Sentinel

### 5. `de-llm` (tae898) — Score: 3
- **Source:** https://github.com/tae898/de-llm
- **Pitch:** Strips the tells that mark text as machine-generated (formatting patterns first, paste-era artifacts last), ordered by measured frequency, with sourced vs. unsourced claims labelled — useful pre-publish pass for agent-drafted content.
- **Suggested owner:** Charlie

## Notes
- Considered but excluded (score < 3 or off-topic for The Construct): `qt-commander` (desktop Qt automation), `conference-partner-mcp` (academic CFP deadlines), `osc-bridge-skill` (music/MIDI), `workforge-mcp` (ChatGPT/Windows gateway), `gandr-mcp` (generic TTS), `notebooklm-skill` (source-grounded Q&A, plausible future Atlas candidate but not distinct enough from existing research tooling this cycle).
- No new listings found on mcp.directory or npm/PyPI keyword search that were both genuinely published in the last 24h and relevant — GitHub topic search (with `created:` filtering) was the only source that reliably surfaced same-day activity; the directory/registry sites appear to lag actual publish dates by several days.
