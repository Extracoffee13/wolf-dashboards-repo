# Skill / MCP / Platform Discovery Sweep — 2026-07-09

Scanned: mcp.directory & modelcontextprotocol.io registry, GitHub (topics `claude-skill`, `mcp-server`), npm (keyword `mcp-server`, `anthropic-skill`), PyPI (keyword `claude-skill`, `mcp`), claude.com/blog and anthropic.com/news.

Freshness note: strict "last 24h" filtering via search is approximate — dates below are the best-available publish/update timestamps found per source. Items older than ~72h were excluded unless they represent an active, still-rolling-out release relevant to the fleet.

## Candidates (score ≥ 3)

### 1. Claude Agent SDK for Python v0.2.114 — score 4
- **Source:** https://github.com/anthropics/claude-agent-sdk-python (released 2026-07-08)
- **Pitch:** Adds a `SessionStore` protocol (with `InMemorySessionStore` + 9 async helpers) and an `effort` field (`low`/`medium`/`high`/`max`) plus structured `ThinkingConfig` types — direct upgrade path for any agent in the fleet built on the SDK.
- **Suggested owner:** Architect (owns the agent framework/infra layer the fleet is built on).

### 2. Claude Code 2.1.204 — score 3
- **Source:** https://code.claude.com/docs/en/changelog (released 2026-07-08)
- **Pitch:** Fixes hook events not streaming during `SessionStart` hooks in headless sessions — directly affects reliability of scheduled/headless routines like this discovery sweep.
- **Suggested owner:** Cornerstone (infra/foundation, owns headless & scheduled-session reliability).

### 3. @playwright/mcp (Playwright Tools for MCP) — score 4
- **Source:** https://www.npmjs.com/package/@playwright/mcp (published ~2026-07-06)
- **Pitch:** MCP server for browser automation via structured accessibility snapshots — lets a research agent drive live web pages (competitor sites, filings portals, dashboards) instead of static fetches.
- **Suggested owner:** Oracle (research agent, would use weekly for live-web verification).

### 4. Financial Datasets MCP Server — score 4
- **Source:** https://docs.financialdatasets.ai/mcp-server
- **Pitch:** Structured MCP access to stock prices, financial statements, SEC filings and news going back 30 years — a ready-made data feed for Hartley Capital-side research instead of hand-rolled scraping.
- **Suggested owner:** Equity (investment research agent).

### 5. Claude Tag for Slack — score 3
- **Source:** https://www.anthropic.com/news (announced 2026-07-06)
- **Pitch:** Native Claude tagging inside Slack conversations for Team/Enterprise plans — could reduce custom glue code around the existing Slack MCP connector for ad-hoc delegation.
- **Suggested owner:** Signal (comms agent, owns the Slack surface).

## Not pursued
Other hits this cycle (ABAP MCP SDK, generic "awesome-mcp-servers" list repos, incremental GitHub MCP server releases) scored ≤2 — no clear weekly use case for The Construct's current stack.
