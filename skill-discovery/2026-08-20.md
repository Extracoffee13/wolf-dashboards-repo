# Skill Discovery Sweep — 2026-08-20

Daily scan of new agent skills, MCP servers, and AI-platform releases from the last 24 hours, scored for relevance to The Construct (Brand 9 Signs + Hartley Capital + agent ecosystem).

## Sources checked
- mcp.directory — **blocked by network egress proxy this run**, could not browse directly
- github.com (topics: `claude-skill`, `mcp-server`, `anthropic-skill`) — reachable, searched by most-recently-created/updated
- npm (keywords: `mcp-server`) — reachable via search, no strong <24h hits found
- pypi (keywords: `claude-skill`, `mcp`) — pypi.org search page returned 403 to the fetcher; fell back to indirect web search
- claude.com/blog and anthropic.com/news — **blocked by network egress proxy this run**, could not browse directly. Known items from this week (Aug 12–13): Claude Cowork (Chrome side panel rename), Claude Tag room-context update, skill/plugin security scanning beta — none are brand-new-today releases, noted for awareness only, not scored below.

## Candidates (score ≥ 3, max 5)

### 1. scientistos — score 4
- **Source:** https://github.com/Ahmad-Jaradat-Space/scientistos
- **Pitch:** Evidence-first research skill for Claude Code/Codex that ties every agent claim to a verifiable evidence graph instead of chat history — the exact discipline our `verify-before-claiming` / `review-build-before-claiming-done` / `b9-knowledge-graph` skills already enforce by hand.
- **Suggested owner:** Oracle

### 2. agentbrain — score 4
- **Source:** github.com/search (topic `mcp-server`, repo `agentbrain`, owner "Matt W" / rightbrain.ai) — "Local-first long-term memory for AI agents: plain Markdown vault + thin MCP server"
- **Pitch:** Durable, file-based cross-session memory an MCP server can read/write directly — a natural backing store for the PRAXIS inbox / `self-improving-loop` pattern instead of hand-appended markdown blocks.
- **Suggested owner:** Keystone

### 3. openanalyst — score 3
- **Source:** github.com/search (topic `mcp-server`, repo `openanalyst`) — data analysis via DuckDB, SQL, and Vega-Lite visualization for agents
- **Pitch:** Lets an agent run SQL/DuckDB queries and render Vega-Lite charts directly from tool calls — useful for turning GSC/GA creative-abrasion pulls or Hartley Capital numbers into charts without a manual export step.
- **Suggested owner:** Ledger

### 4. dunsocial — score 3
- **Source:** github.com/search (topic `mcp-server`, repo `dunsocial`) — "AI social media scheduling, 43-tool MCP server, CLI, API, platforms, workflows"
- **Pitch:** Broad multi-platform scheduling/posting toolset that overlaps with our `multi-channel-social-posting` / `linkedin-posting` skills — worth a scoped trial rather than a swap-in.
- **Suggested owner:** Signal

### 5. secureflows-mcp-server — score 3
- **Source:** github.com/search (topic `mcp-server`, repo `secureflows-mcp-server`) — wraps an "ai-safe / ai-optional" API surface and scaffolds AI-agent integrations
- **Pitch:** Pre-built guardrail scaffolding for wiring new AI-agent integrations safely — niche, but relevant the next time we bolt a new MCP server onto the fleet.
- **Suggested owner:** Sentinel

## Notes / caveats
- `mcp.directory`, `claude.com`, and `anthropic.com` were all blocked by the egress proxy this run, so this sweep leans heavily on GitHub's live search index (which does surface repos created minutes before the scan) plus indirect web search for npm/pypi. Coverage of the official Anthropic blog/news and the mcp.directory listing is a known gap for today.
- No standout new *Anthropic-platform* release (model, Claude Code feature, API change) surfaced in the last 24h distinct from the known Aug 12–13 items above.
