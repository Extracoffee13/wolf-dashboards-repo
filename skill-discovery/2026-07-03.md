# Skill Discovery Sweep — 2026-07-03

Scanned: mcp.directory (blocked, 403 — see Notes), modelcontextprotocol.io, GitHub (topics `mcp-server`, `claude-skill`, `anthropic-skill`, created/updated in last 24h), npm (keywords `mcp-server`, `anthropic-skill`), PyPI (keyword `claude-skill`, JS-rendered search page returned no results — see Notes), claude.com/blog, anthropic.com/news.

## Candidates (score ≥ 3)

### 1. sideguard — score 5
- **Source:** https://github.com/alisaitteke/sideguard
- **Pitch:** Fail-closed, human-in-the-loop approval gate that intercepts shell commands and MCP tool calls from Claude Code/Cursor against a local YAML policy before they execute, with a SQLite audit trail.
- **Published:** v0.1.6, 2026-07-03
- **Suggested owner:** Sentinel — this is a drop-in upgrade for exactly Sentinel's job (real-time risk monitor / circuit breaker) and could wrap approval for any Construct agent's shell or MCP calls, not just WOLF's.

### 2. prediction-markets-skill — score 4
- **Source:** https://github.com/azazelitto21/prediction-markets-skill
- **Pitch:** Read-only, zero-dependency, no-API-key skill giving agents auditable access to Kalshi and Polymarket market data (discovery, orderbooks, price history, cross-venue matching) without hallucinating endpoints.
- **Published:** 2026-07-02
- **Suggested owner:** WOLF — a new signal-input surface (event/prediction-market pricing) WOLF's committee could poll alongside its existing congressional-trade and macro feeds.

### 3. fable-token-saver — score 4
- **Source:** https://github.com/vincemakes/fable-token-saver
- **Pitch:** Tiered model-orchestration harness that routes implementation work to cheaper models and reserves the top tier for judgment-only planning/review, cutting strongest-tier quota use 34–88% via objective typecheck/test gates.
- **Published:** 2026-07-03
- **Suggested owner:** Architect — fleet-wide cost control that every agent in the Construct (Brand 9 Signs + Hartley Capital + WOLF) would draw on weekly, best owned by the agent responsible for system-level architecture decisions.

### 4. opzyai-mcp — score 4
- **Source:** https://github.com/thfothijn/opzyai-mcp
- **Pitch:** Fully local MCP server that scans working tree, `.env` files, and git history for leaked secrets/credentials and cross-checks dependencies for CVEs, with an offline mode and zero code sent off-machine.
- **Published:** 2026-07-03
- **Suggested owner:** Cipher — directly protects the credentials and API keys the Construct's financial (Hartley Capital) and trading (WOLF) integrations depend on.

### 5. claims-ledger — score 3
- **Source:** https://github.com/isatimur/claims-ledger
- **Pitch:** CI-gated "claims ledger" that forces every documented claim or agent decision to carry a machine-verifiable pointer to its source (git/doc/ADR/GitHub/etc.) and fails builds when references go stale.
- **Published:** 2026-07-03
- **Suggested owner:** Ledger — a natural extension of PRAXIS's evidence-based packet logging; would let the fleet's `lesson`/`decision` claims be source-anchored and CI-verified instead of trusted at face value.

## Notes on sources
- `mcp.directory` returned HTTP 403 to automated fetch; could not confirm its listings directly this cycle.
- PyPI's search UI is JS-rendered and returned no results via fetch; no PyPI-sourced candidates this cycle as a result — npm and GitHub carried the signal instead.
- Anthropic's biggest news this window (Claude Sonnet 5 as default in Claude Code, Fable 5/Mythos 5 export-control lift, Claude Science launch) all predate the 24h window or are platform-level rather than a discrete skill/MCP-server release, so they're noted here but not scored as candidates.
- Notable but scored below 3 / out of scope: `job-verify` (recruiter-scam OSINT, not relevant to the Construct), `@zereight/mcp-gitlab` (GitLab MCP — Construct runs on GitHub), `@mcp-b/transports` + `@mcp-b/webmcp-ts-sdk` v4.0.0 (browser-MCP transports, no current browser-agent use case).
