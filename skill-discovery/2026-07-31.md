# Skill Discovery Sweep — 2026-07-31

Sources checked: mcp.directory listings, modelcontextprotocol.io blog/spec updates, GitHub (topics `claude-skill`, `mcp-server`, `anthropic-skill`, filtered `created:>=2026-07-29/30`), npm (keyword `mcp-server`, `anthropic-skill` — npmjs.com search blocked 403 this run), PyPI (keyword `claude-skill`, `mcp` — pypi.org search blocked 403 this run), claude.com/blog and anthropic.com/news (both returned 403 to direct fetch this run; covered via web search instead).

Scope note: GitHub's `created:` date filter gives reliable freshness; npm/PyPI registry search pages 403'd for this session's fetcher, so those two channels were covered only via general web search rather than a true "last 24h" listing. Flagged below where recency is filter-confirmed vs. approximate.

## Candidates (score ≥ 3)

### 1. claude-seo-kit — score 5
- **Source:** https://github.com/FreeAutomation-Tech/claude-seo-kit
- **Recency:** confirmed via GitHub `created:>=2026-07-30`, topic `mcp-server`
- **Pitch:** MIT-licensed, dependency-light (just `httpx`) Python engine behind 7 Claude Code skills for technical/content/structured-data/international/sitemap SEO audits plus a GEO/AI-search LLM-citability scorer — each producing a 0-100 score with prioritized fixes, no API keys required.
- **Why it matters:** the GEO/AI-search citability scorer is directly in the lane of the `ai-visibility-answer-shaping` skill and Pulse's owned-presence SEO work — a candidate drop-in for automating the "measure" step of the Sauce loop without a paid API dependency.
- **Suggested owner:** Pulse

### 2. presales-agent-skill — score 3
- **Source:** https://github.com/namit-jpg/presales-agent-skill
- **Recency:** confirmed via GitHub `created:>=2026-07-30`, topic `claude-skill`
- **Pitch:** Claude Code plugin that runs a guided discovery interview and turns it into delivery-ready artifacts — scope matrix, sprint plan, timeline, Excel export — built around a Salesforce presales workflow.
- **Why it matters:** niche fit, but the discovery→scope-matrix→timeline pattern maps onto client onboarding for Brand 9 Signs / Hartley Capital engagements if adapted off Salesforce specifics.
- **Suggested owner:** Venture

### 3. ask-me-questions — score 3
- **Source:** https://github.com/JuliusGruber/ask-me-questions
- **Recency:** confirmed via GitHub `created:>=2026-07-30`, topic `claude-skill`
- **Pitch:** domain-agnostic Claude Code skill that interviews the user one question at a time to turn a rough prompt draft into a well-specified one, researching context first to minimize needless questions (self-reported ~94% accuracy across iterations).
- **Why it matters:** general-purpose brief-sharpening step any Construct agent could invoke before kicking off ambiguous work (e.g., before Keystone scoping or Ledger modeling requests) — low priority since it's a workflow nicety, not a new data/capability source.
- **Suggested owner:** AP

## Noted but excluded (< 3 or recency unconfirmed)
- `Auriti-Labs/geo-optimizer-skill` and `SNLabat/SEO-GEO-AEO-Skill` — same AEO/GEO space as #1 above, plausibly stronger, but no confirmed creation/update date inside the last 24-48h this sweep — re-check next cycle.
- SurgeGraph AEO/GEO Claude Code skill — relevant but its public launch dates to mid-May 2026, well outside window.
- MCP spec 2026-07-28 release candidate (stateless protocol layer) — infrastructure-level, not agent-facing; no action needed from The Construct's agents.
- Financial-data MCP servers (Financial Modeling Prep, Alpha Vantage, MarketXLS) — mature, already-established players surfaced by generic search, not new in this window; worth a dedicated Ledger/Alpha evaluation pass separate from a discovery sweep.
- Multi-agent orchestration repos (claude-swarm, claude-team-orchestration, metaswarm, etc.) — active ecosystem, none with confirmed creation in the last 24-48h; several look mature enough to warrant a deeper one-off review of whether The Construct's own swarm tooling should borrow patterns (worth flagging to Keystone separately, not as a discovery-sweep item).

## Bottom line
3 candidates cleared the ≥3 bar this cycle (below the 5-candidate cap) — a thin day for genuinely new, dated releases. The one standout is `claude-seo-kit` for Pulse.
