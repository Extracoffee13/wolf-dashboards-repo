# Skill Discovery Sweep — 2026-07-25

Scope: mcp.directory, modelcontextprotocol.io, GitHub (topics: claude-skill, mcp-server,
anthropic-skill), npm (anthropic-skill, mcp-server), PyPI (claude-skill, mcp), and
claude.com/blog + anthropic.com/news, for releases in roughly the last 24-48 hours.

**Access note:** mcp.directory returned a 403 (blocked to automated fetch) — coverage
of that source is via search-engine cache only, not a direct listing pull. GitHub
topic pages were reachable and are the strongest signal in this sweep.

## Candidates (score ≥ 3)

### 1. Timeline MCP — content calendar / multi-platform scheduler
- **Source:** https://glama.ai/mcp/servers/@derekalia/timeline-mcp
- **Pitch:** Lets an agent schedule and manage posts across X, LinkedIn, Instagram, TikTok, YouTube, and Reddit from natural language, organized into campaign "tracks."
- **Score:** 4 — new capability the team would use weekly; would let `multi-channel-social-posting` and `linkedin-posting` skills schedule/queue instead of driving each platform's composer live via browser automation.
- **Suggested owner:** Signal

### 2. mcp-gsc — Google Search Console MCP server
- **Source:** https://github.com/AminForou/mcp-gsc
- **Pitch:** Exposes Google Search Console data (queries, pages, index coverage) as MCP tools so an agent can pull real ranking/impression data instead of scraping the GSC UI.
- **Score:** 4 — direct weekly-use upgrade for owned-presence SEO/GEO reporting.
- **Suggested owner:** Signal

### 3. web-check-mcp — OSINT site-health checker
- **Source:** https://github.com/AMEOBIUS-space/web-check-mcp
- **Pitch:** Wraps the open-source Web Check OSINT API as an MCP server — 31 checks (SSL, DNS, headers, ports, WHOIS) run in parallel, no auth required, zero-dependency stdio server. Repo shows a same-day (2026-07-25) verification note.
- **Score:** 3 — niche but useful for periodic security/health audits of brand9signs.com and other owned properties.
- **Suggested owner:** Sentinel

### 4. memtomem-stm — short-term memory proxy for AI agents
- **Source:** https://github.com/memtomem/memtomem-stm
- **Pitch:** A proxy gateway that gives agents proactive short-term memory surfacing, i.e. injects relevant prior context without an explicit retrieval call.
- **Score:** 3 — interesting fit for the PRAXIS inbox / cross-agent memory pattern already in use in this repo, but unproven and would need evaluation before adoption.
- **Suggested owner:** Architect

## Considered but excluded (< 3 or too stale to confirm as new)
- Meta's official Facebook/Instagram Ads MCP — real and relevant to Signal/Vector, but shipped April 2026, not a last-24h release.
- SEOcrawl AI (74 SEO+GEO tools over GSC/GA4) — commercial, overlaps heavily with #2 above, freshness unconfirmed.
- `atelier` (pixel-art studio over MCP), `ableton-osc-mcp`, `base-agent-airdrop-mcp`, `amenbo` — recently-touched GitHub `mcp-server` topic entries, but off-thesis for Brand 9 Signs / Hartley Capital (music production, crypto airdrops, niche web scraping). Scored 1-2, dropped.

## Notes on methodology
"Published in the last 24 hours" is difficult to verify precisely through search-engine
results, which rarely surface exact commit/publish timestamps. Recency above is
best-effort: confirmed same-day for web-check-mcp via an in-repo timestamp note, and
inferred from "most recently updated" sort order elsewhere. Treat items here as
this week's candidates, not guaranteed last-24h drops.
