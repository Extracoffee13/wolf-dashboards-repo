# Skill Discovery Sweep — 2026-08-27

Scanned: mcp.directory, GitHub (topics `mcp-server`, `claude-skill`, created in the last 24h),
npm (`anthropic-skill`, `mcp-server` keywords), PyPI (`claude-skill`, `mcp` keywords),
claude.com/blog, anthropic.com/news (blocked by egress proxy — see note below).

Note: anthropic.com/news could not be fetched directly (network egress proxy blocks
`www.anthropic.com`); coverage of that source this cycle came from search snippets only,
and nothing from it fell inside the 24h window.

## Candidates (score ≥ 3)

### 1. away-mode — score 4
- **Source**: https://github.com/demircigoksu/away-mode
- **Pitch**: A Claude Code skill that keeps working through a task list unattended, checking off items while you're away from the keyboard — same shape as our WOLF/Hermes autonomous-loop pattern, but packaged as a portable skill.
- **Suggested owner**: WOLF

### 2. deck-editor-skill — score 4
- **Source**: https://github.com/ejbo/deck-editor-skill
- **Pitch**: Slide decks that ship as a single self-editing HTML file — directly usable for WOLF Command Center-style decks (we already keep deck-slide-*.jpg assets in this repo) without a separate slide toolchain.
- **Suggested owner**: Charlie

### 3. mida-plugin — score 3
- **Source**: https://github.com/mida-so/mida-plugin
- **Pitch**: MCP server for running A/B tests and on-site personalization directly from an agent — fits the ANS/PRAGMA flywheel's conversion-loop work on brand9signs.com without hand-rolling test infrastructure.
- **Suggested owner**: Signal

### 4. pdf-toolkit — score 3
- **Source**: https://github.com/khusan-1-lox/pdf-toolkit
- **Pitch**: Local, no-cloud PDF MCP server (25 tools) for parsing/editing PDFs — useful for Hartley Capital document work where sending financial PDFs to a cloud service isn't desirable.
- **Suggested owner**: Ledger

### 5. claude-skill-polylint — score 3
- **Source**: https://github.com/lucas-lima-s/claude-skill-polylint
- **Pitch**: Three-phase lint runner for Claude Code with approval-gated fixes — a safer default for agents (Hermes jobs, ATLAS/PRAGMA scripts) that currently apply lint fixes without a review gate.
- **Suggested owner**: Forge

## Items reviewed but scored below threshold
- `octowatch-mcp-server` (DLP cloud monitoring) — score 1, no DLP use case in the ecosystem.
- `TheBestCo/bestprice-mcp` (price comparison) — score 1, not applicable to current ventures.
- `hnmcp` (Hacker News MCP) — score 2, low-priority research curiosity.
- `node-graph-skills` (DCC node graph from spec) — score 2, targets Houdini/Nuke-style pipelines, not our 3ds Max/Blender MAXScript workflow.
- `bottari` (Claude/Codex environment backup+sync) — score 2, interesting for Hermes reliability but not urgent given existing worker-queue work.
- Claude Skills API GA, Agent Plugins 1.0.0 standard, `/design` research preview — all real and relevant, but confirmed dates (Aug 20, Aug 6, Aug 17) fall outside the 24h window for this sweep.

## Method note
GitHub's `created:>` date filter was the only reliable way to bound results to the last
24 hours; npm/PyPI keyword search and the generic web search engine do not expose a clean
recency filter, so those sources are weighted more lightly this cycle pending a better
feed (e.g. npm/PyPI "recently published" RSS).
