# Skill / MCP / Platform Discovery Sweep — 2026-09-01

**Run by:** AP (scheduled sweep)
**Sources checked:** GitHub (topics `mcp-server`, `claude-skill`, sorted by creation date, filtered to items created within the last 24h), npm (`anthropic-skill`, `mcp-server`), PyPI (`claude-skill`, `mcp`), general web search for MCP/skill releases and claude.com/blog · anthropic.com/news headlines.

**Methodology note:** `www.anthropic.com` and `claude.com` are blocked by this session's network egress proxy, so claude.com/blog and anthropic.com/news could not be fetched directly this cycle — checked via web search instead, which surfaced nothing dated in the last 24h beyond previously-known items (MCP 2026-07-28 spec, Claude Partner Network, Apple Xcode integration — all older than this window). GitHub topic search with `created:>2026-08-31` gave the highest-signal, most reliably time-boxed results this cycle, so this sweep leans on it.

## Candidates (score ≥ 3)

### 1. `google-search-console-mcp` (navidmoazzez) — Score: 5
- **Source:** https://github.com/navidmoazzez/google-search-console-mcp
- **Pitch:** TypeScript MCP server exposing GSC search analytics, URL inspection, sitemap management, and property verification directly to any MCP client — a drop-in replacement for the hand-rolled GSC API calls the creative-abrasion puller currently makes, and it removes the exact OAuth/quota-project friction documented in `b9-google-api-access-setup`.
- **Suggested owner:** Signal (analytics/marketing-signal role; owns the GSC/GA pull per `gsc-ga-creative-abrasion`).

### 2. `sage-audit` (tmolavi) — Score: 4
- **Source:** https://github.com/tmolavi/sage-audit
- **Pitch:** "Unified 3-Pillar Audit Engine" scoring a site across classic SEO, Entity AEO, and Generative Engine Optimization in one pass (CLI + Python lib + MCP server) — lines up directly with the AAIR scoring work in `ai-visibility-answer-shaping` and could serve as an independent cross-check on the in-house AAIR measurement.
- **Suggested owner:** Signal (owns AI-visibility measurement; best guess, no confirmed roster mapping for AEO/GEO tooling specifically).

### 3. `mcp-freshness` (waseemnasir2k26) — Score: 3
- **Source:** https://github.com/waseemnasir2k26/mcp-freshness
- **Pitch:** Zero-dependency CLI that scores every MCP server in a config for liveness/staleness (still starts, still answers, repo activity, archived status) and emits a table/JSON/shields.io badge — useful given how many MCP servers The Construct has already accumulated across agents, to catch silent rot before an agent hits a dead connector mid-task.
- **Suggested owner:** Architect (platform/infra role, owns the MCP roster).

### 4. `reddit-research-agent` (navidmoazzez) — Score: 3
- **Source:** https://github.com/navidmoazzez/reddit-research-agent
- **Pitch:** Read-only Reddit MCP server that finds the subreddits actually discussing a topic and ranks threads by real conversation (not upvotes), returning verbatim quotes tagged by intent with permalinks — a ready-made voice-of-customer source for buyer research (complements `pragma-buyer-voice`) without building a Reddit scraper in-house.
- **Suggested owner:** Oracle (research/intelligence role; best guess).

### 5. `ui-audit-method` (rhino-ty) — Score: 3
- **Source:** https://github.com/rhino-ty/ui-audit-method
- **Pitch:** Claude Code skill for auditing a UI codebase by measurement rather than impression — counting pitfalls, render verification, a documented display-scaling screenshot trap, and a settled baseline for re-checks — directly useful for the recurring "does the build actually look right" verification problem across ANS/Structa/B9 site work (echoes `review-build-before-claiming-done` but with a more rigorous method).
- **Suggested owner:** Architect (owns build/QA methodology across the site-generation pipelines).

## Notable but excluded (score < 3 or not actionable)
- `google-photos-mcp`, `vimeo-mcp`, `podcastindex-mcp`, `bluesky-mcp`, `mastodon-mcp`, `substack-mcp`, `whatsapp-mcp` (all navidmoazzez, same day) — well-built MCP servers for platforms The Construct doesn't currently operate on.
- `saferico-cli`, `gti-lookup` — Solidity/threat-intel security scanners, outside domain (no smart-contract or infosec work in scope).
- `icons8-mcp-server`, `mcpforge`, `mcp-ollama`, `projectfs` — general-purpose dev tooling, no immediate Construct use case this cycle.
- `geo-optimizer-skill` (Auriti-Labs) — open-source AEO/GEO toolkit, functionally overlaps with `sage-audit` above and is not confirmed as posted in the last 24h; flagged for a future cycle if `sage-audit` doesn't pan out.
- Various genpark-* commerce/influencer skills, `destiny-skill` (I Ching divination), `wow-vanilla-server-mac`, `birokrasi-pt-indonesia` — no relevance to Brand 9 Signs, Hartley Capital, or the agent ecosystem.
