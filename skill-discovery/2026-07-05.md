# Skill Discovery Sweep — 2026-07-05

Scanned: mcp.directory (blocked, 403 — see note), modelcontextprotocol.io (blocked, 403 — see note), npm registry search API (keywords: `anthropic-skill`, `mcp-server`), PyPI search (keyword: `mcp`), GitHub search (topics `claude-skill` / `mcp-server`), anthropic.com/news, claude.com/blog.

**Note:** `mcp.directory` and `modelcontextprotocol.io` returned HTTP 403 to direct fetch (likely bot-blocking); coverage of those two sources came via secondary web search results referencing them rather than a direct listing crawl. Everything else was reachable directly.

## Candidates (score ≥ 3)

### 1. pdf-it-mcp — score 5
- Source: https://www.npmjs.com/package/pdf-it-mcp
- Published/updated: 2026-07-04 (v2.0.0)
- Pitch: MCP server + Claude Code skill that renders markdown straight into designed PDFs (cover pages, TOC, code-block formatting) — a direct upgrade path for turning WOLF/Praxis markdown reports into investor-grade PDFs without a manual export step.
- Suggested owner: **Ledger**

### 2. @cyanheads/mcp-ts-core — score 4
- Source: https://www.npmjs.com/package/@cyanheads/mcp-ts-core
- Published/updated: 2026-07-05
- Pitch: Agent-native TypeScript framework that handles transport, auth, storage, logging, and lifecycle for a new MCP server so you only write tools/resources/prompts — scaffolds via `bunx @cyanheads/mcp-ts-core init`, complete with CLAUDE.md and Agent Skills wired in. Cuts the boilerplate every time The Construct stands up a bespoke internal MCP server.
- Suggested owner: **Forge**

### 3. MCP Specification 2026-07-28 Release Candidate — score 3
- Source: https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/
- Published: 2026-07 (RC ahead of a July 28 final release)
- Pitch: Largest MCP protocol revision since launch — stateless core over plain HTTP, server-rendered UI via MCP Apps, a Tasks extension for long-running work, and OAuth/OIDC-aligned auth. Worth tracking now since every MCP server The Construct depends on (GitHub, Slack, Gmail, Higgsfield, Canva, Google Drive) will eventually need to track this spec bump.
- Suggested owner: **Architect**

### 4. surf-skill — score 3
- Source: https://www.npmjs.com/package/surf-skill
- Published/updated: 2026-07-03 (v4.1.0)
- Pitch: Multi-provider web search skill (Tavily + Parallel AI) with auto-fallback and multi-key rotation — a resilience backstop for research tasks if a primary search path rate-limits or degrades.
- Suggested owner: **Oracle**

### 5. metaharness — score 3
- Source: https://github.com/ruvnet/metaharness
- Published/updated: 2026-07 (active, npm `@metaharness` scope live)
- Pitch: Scaffolds a fully-branded agent harness (own CLI, MCP server, memory, learning loop) in one shot — relevant if The Construct ever needs to spin up a new named agent (in the Vector/Forge/Signal mold) with its own packaged tooling rather than hand-rolling one.
- Suggested owner: **Architect**

## Considered, below threshold (score < 3)
- `@zereight/mcp-gitlab` (npm, updated 2026-07-05) — freshest hit of the sweep, but The Construct's repo work runs on GitHub, not GitLab. Score 2.
- `comfyui-mcp` (npm, updated 2026-07-03) — image-gen MCP server; overlaps with existing Higgsfield capability already connected in this environment. Score 2.
- `@ironbee-ai/devtools`, `agent-device`, `dimctl-skill` — surfaced in the sweep but no clear, verifiable fit for an existing Construct workflow. Score 1–2.

## Summary
5 candidates scored ≥3. Top pick: **pdf-it-mcp** (score 5) — closest thing to a drop-in upgrade this cycle, directly usable for turning existing markdown reports (praxis reviews, tax reports) into polished PDFs.
