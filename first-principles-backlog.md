# First-Principles Backlog

Open questions for the daily First-Principles Spike. Pick one per run, mark it
`[spiked YYYY-MM-DD]` when done (leave it in place — spiked questions can be
revisited later with new primitives).

This file did not exist before 2026-08-07; it was created empty by the first
spike run, which then generated its own question from current Construct work
since the backlog had nothing queued yet.

## Open questions

- [spiked 2026-08-07] How should a daily/weekly drawdown circuit-breaker
  threshold be derived from first principles for an automated trading
  system, and how does WOLF's actual -3% daily / -7% weekly pair compare to
  standard risk-management practice?
- Why does commercial sign fabrication pricing converge on per-square-foot
  rather than per-letter or per-hour, and does that hold at the extremes
  (very small vs. very large cabinets)?
- What is the first-principles-correct way to size a real-estate cap rate
  relative to the risk-free rate, and how does that map to how Hartley
  Capital should read cap-rate compression/expansion signals?
- Why might a multi-agent system (like the Construct's C-suite agents)
  benefit from a shared append-only log (praxis-inbox.md) instead of direct
  point-to-point messages between agents?
- From first principles, what permit-cycle-time drivers should predict which
  municipalities are slow vs. fast for sign permitting, before checking any
  actual jurisdiction data?

## Notes

Add new candidate questions here as they come up during other Construct
work. The spike doesn't require the list to be exhaustive — generating a
fresh question inline is fine when the backlog is empty or stale.
