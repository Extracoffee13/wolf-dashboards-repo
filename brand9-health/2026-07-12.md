# Brand 9 Site Health Report — 2026-07-12

## Overall Status: 🔴 RED — Automated verification blocked (403 on all paths)

Every request made to `brand9signs.com` by this check's tooling (WebFetch, and a
direct `curl` test) returned **HTTP 403 Forbidden** — including `/robots.txt`,
which is normally publicly accessible on any live site. That pattern points to
a WAF / bot-protection rule (e.g. Cloudflare) blocking this monitoring
environment's IP/fetcher, rather than confirmed evidence that real visitors are
seeing errors. However, per the report's severity rubric (any 4xx counts as
RED) and because **none of the 6 required checks could be completed**, this
run is scored RED and flagged for human follow-up with a real browser.

## Checks

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ❌ Could not verify — 403 Forbidden on `https://brand9signs.com/` and `https://www.brand9signs.com/` |
| 2 | `/product-category/marketing-branding/` — 16-tile nav row + 8 product cards + Motion Films block | ❌ Could not verify — 403 Forbidden. Page **does exist and is indexed** ("Marketing & Branding Archives \| Brand 9 Signs" found via search), but content/layout could not be inspected |
| 3 | 5 random product pages — HTTP 200, image, price | ❌ Could not verify — sampled `/product/military-base-signs/` returned 403 Forbidden; no other product URLs could be fetched to sample |
| 4 | Yoast SEO on Motion Films product page (focus keyphrase, meta description, SEO title, no placeholder leakage) | ❌ Could not verify — page content inaccessible; could not even confirm the "Motion Films" product page's exact URL |
| 5 | OG image for homepage renders | ❌ Could not verify — homepage HTML/meta inaccessible |
| 6 | New posts/products in last 24h | ❌ Could not verify via direct fetch. Indirect signal: search index shows no new URLs beyond previously-seen pages (`/`, `/jacksonville-sign-company/`, `/store-effective-signage-branding/`, `/product/military-base-signs/`, `/product/graphics/`, blog posts on evolution/HQ/branding-vs-marketing), but search-index freshness lags real-time so this is not conclusive |

## Diagnostics performed

- `WebFetch` on homepage, category page, a product page, `robots.txt`, and the
  `www` subdomain — **all returned 403 Forbidden**, no response body.
- Direct `curl` from the sandbox also failed, but at the *proxy* layer
  (`CONNECT tunnel failed, response 403` for brand9signs.com **and** for
  unrelated control domains like google.com/example.com) — confirming the
  sandbox's own outbound proxy blocks general Bash-level HTTPS egress
  entirely. This is a pre-existing environment restriction, separate from the
  WebFetch-tool 403s above.
- `WebSearch` (Google-indexed snapshot) confirms the site is live and indexed
  with expected pages, including a `Marketing & Branding Archives` page at the
  exact URL this check targets — so the domain itself is not obviously down or
  deregistered.

## Headline finding

**This health check could not directly verify any of the 6 target conditions
today** because `brand9signs.com` returned 403 Forbidden to every automated
fetch attempt (including `robots.txt`). Search-engine index snapshots suggest
the site is otherwise up and serving normal content to real crawlers, which
points to this being a bot-protection/WAF block against this specific
monitoring tool rather than a confirmed site outage — but that distinction
itself needs a human to confirm by loading the site in an actual browser.

## Recommended follow-up

1. Load `https://brand9signs.com/product-category/marketing-branding/` in a
   real browser to confirm the 16-tile nav + 8 product cards + Motion Films
   block are actually rendering.
2. Check whether Cloudflare/Wordfence (or similar) has a bot-protection rule
   that would also block legitimate monitoring/uptime services, search
   console re-crawls, or social-preview crawlers (which would explain OG-image
   preview failures on shares).
3. Re-run this automated check tomorrow; if 403s persist, the block is likely
   durable (IP range or fetcher fingerprint) and this daily check needs an
   allowlist exception or a different verification method (e.g. an
   uptime-monitoring service the site already trusts).
