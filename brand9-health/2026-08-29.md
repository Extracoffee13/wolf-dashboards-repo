# Brand 9 Site Health Check — 2026-08-29

**Overall status: 🔴 BLOCKED — audit could not run**

This is not a confirmed site outage. It is a tooling/network failure in this session:
outbound access to `brand9signs.com` is blocked at the environment's egress-gateway
policy level, so none of the six requested checks could be executed as specified.

## What happened

- `WebFetch` on `https://brand9signs.com` and `https://brand9signs.com/product-category/marketing-branding/`
  both returned `EGRESS_BLOCKED` ("Access to brand9signs.com is blocked by the network egress proxy").
- Direct `curl` from this session's shell failed identically: `CONNECT tunnel failed, response 403`.
- The proxy's own status endpoint confirms this was a deliberate policy denial, not a transient
  network blip: `recentRelayFailures` logged `connect_rejected` / "gateway answered 403 to CONNECT
  (policy denial or upstream failure)" for `brand9signs.com:443` at 2026-08-29T16:03:57Z.
- `WebSearch` (Google index, not a live fetch) does work from this session and confirms
  `brand9signs.com/product-category/marketing-branding/`, `brand9signs.com/portfolio/`, and several
  `/product/<slug>/` URLs are indexed — but indexed snippets cannot verify live HTTP status, console
  errors, embed rendering, image/price presence, or current Yoast field content. None of that is
  treated as a substitute for the real checks below.

## Checklist status (all unverifiable this run)

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | **NOT RUN** — no live fetch possible |
| 2 | `/product-category/marketing-branding/` shows 16-tile nav + 8 product cards + Motion Films block | **NOT RUN** |
| 3 | 5 random product pages: HTTP 200 + image + price | **NOT RUN** |
| 4 | Yoast SEO on Motion Films product page (keyphrase/meta description/SEO title, no placeholder leakage) | **NOT RUN** |
| 5 | Homepage OG image renders | **NOT RUN** |
| 6 | New posts/products in last 24h | **NOT RUN** |

## Note on site architecture assumptions

Local skill documentation for this workspace (`b9-website-architecture`, dated 2026-08-27) describes
brand9signs.com as having been rebuilt as a **static HTML site** (no WooCommerce/Yoast-driven
product pages, no live WordPress admin) as of the week of 2026-08-04/05, served from a
`brand9signs-static-build` directory, with `/product/<slug>/` as **service/category** pages (not
priced e-commerce products) and `/portfolio/` as the category hub (accordion list, not a 16-tile nav
+ 8 product cards + Motion Films layout). This is inconsistent with several assumptions in today's
check list (WooCommerce-style product pages with prices, Yoast meta fields, a "Marketing/Branding"
category with a specific tile/card layout). This report does not resolve that discrepancy — it's
flagged here so a human can confirm which description of the live site is current before the next
run, and so the check list can be corrected if it's stale.

## Recommendation

This session has no path to brand9signs.com — WebFetch is egress-blocked and the shell has no
network route to the host. Real checks need to run from wherever the site skills say live
verification actually happens for this project (a Hermes-driven authenticated browser / Mac session,
per `b9-site-health-sentinel` and `b9-website-architecture`), not from this cloud session. Route this
check there, or allowlist `brand9signs.com` for this environment's egress policy if direct fetch is
intended to work from here.
