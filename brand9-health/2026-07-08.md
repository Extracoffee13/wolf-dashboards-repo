# Brand 9 Signs — Daily Site Health Check
**Date:** 2026-07-08
**Target:** https://brand9signs.com
**Overall status:** 🔴 BLOCKED — check could not be executed

## Summary

The audit could not run. Every outbound request to `brand9signs.com` from this
environment — via the WebFetch tool, a headless-Chromium (Playwright) session,
and a raw `curl` — was rejected at the network-egress layer with **403 on the
CONNECT tunnel**, before any request reached the actual WordPress site. The
proxy status endpoint (`/__agentproxy/status`) confirms this is a policy
denial, not a site-side error:

```
"kind": "connect_rejected",
"detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
"host": "brand9signs.com:443"
```

For comparison, an unrelated host (`www.google.com:443`) was denied the same
way in the same window, indicating this session's egress allowlist does not
currently include `brand9signs.com` (or general internet hosts) — this is an
environment/session configuration issue, not evidence that the live site is
down. Per the proxy runbook, policy denials (403/407) should be reported
rather than retried, so no further fetch attempts were made against the
domain.

A `WebSearch` for "brand9signs.com" (which queries a search index rather than
fetching the site directly) did resolve, returning the expected homepage
title *"Brand 9 Signs | Homebuilder, Military & Development Signage"* — a
weak signal that the site is indexed/reachable from the public internet, but
this does **not** substitute for the live checks requested and cannot confirm
console errors, embed rendering, product pages, or Yoast SEO fields.

## Checklist status

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⚠️ NOT RUN — egress blocked |
| 2 | `/product-category/marketing-branding/` shows 16-tile nav + 8 product cards + Motion Films block | ⚠️ NOT RUN — egress blocked |
| 3 | 5 random product pages: HTTP 200, image, price | ⚠️ NOT RUN — egress blocked |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | ⚠️ NOT RUN — egress blocked |
| 5 | OG image for homepage renders | ⚠️ NOT RUN — egress blocked |
| 6 | New posts/products published in last 24h | ⚠️ NOT RUN — egress blocked |

## Root cause

This session's outbound network policy does not currently permit connections
to `brand9signs.com` (or to general internet hosts like `www.google.com`).
This is almost certainly an environment/session network-policy setting, not a
finding about Brand 9 Signs' actual infrastructure.

## Recommended action

- Re-run this check from a session/environment whose network policy allows
  egress to `brand9signs.com`, or add the domain to this environment's
  allowlist.
- If this environment is expected to have general internet access for daily
  health checks, flag the policy gap to whoever configured the scheduled
  routine/environment.
