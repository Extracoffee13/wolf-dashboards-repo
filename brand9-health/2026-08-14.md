# Brand 9 Site Health Report — 2026-08-14

## Overall status: 🔴 RED

The scheduled 6-step audit could not be executed as specified. Two independent problems surfaced, both blocking:

1. **This session has no network path to brand9signs.com at all.** Every `WebFetch` call to `https://brand9signs.com/*` failed immediately with `EGRESS_BLOCKED` (network egress proxy on this repo-scoped session denies the domain outright — this is before even hitting the site's own sgcaptcha bot-shield). No page content, headers, or status codes could be observed directly.
2. **The audit's premise does not match the live site's actual architecture.** Steps 2–4 of the configured check (a `/product-category/marketing-branding/` page with a 16-tile nav + 8 product cards, 5 random "product" pages with prices, and Yoast SEO fields on a "Motion Films product page") describe a WordPress/WooCommerce store. Per verified internal documentation (`b9-site-deploy`, confirmed 2026-08-08) brand9signs.com is a **static HTML site (~357 pages, folder-per-slug) deployed to DreamHost by Hermes — it is explicitly NOT WordPress**, has no WooCommerce product catalog, and the legacy `wp-admin`/Yoast backend is dead (doesn't serve the live site). WebSearch confirms the live site is Brand 9 Signs, a sign fabrication/installation company (community monuments, channel letters, wayfinding, construction signage) with service/answer pages — not a marketing-collateral e-commerce catalog.

### What was actually checked

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, motion film embed renders | ❌ Not verifiable — egress blocked to brand9signs.com from this session |
| 2 | `/product-category/marketing-branding/` tile/card layout | ❌ Not verifiable — egress blocked; also this URL pattern likely doesn't exist on the current static build (no WooCommerce category structure) |
| 3 | 5 random product pages, HTTP 200 + image + price | ❌ Not verifiable — egress blocked; the current site has no "product" pages with prices (it's a service/signage site, not a store) |
| 4 | Yoast SEO meta on Motion Films product page | ❌ Not verifiable — egress blocked; Yoast is not the SEO source of truth for the live static build |
| 5 | OG image on homepage | ❌ Not verifiable — egress blocked |
| 6 | New posts/products in last 24h | ❌ Not verifiable — no site access from this session |

WebSearch (not blocked) returned two live URLs confirming the site is up and indexed:
- `https://brand9signs.com/sign-company-orange-park-fl/`
- `https://brand9signs.com/jacksonville-sign-company/`

That's the only confirmation available this run: the domain resolves and Google has indexed pages, nothing more.

## Headline finding

**This routine cannot produce a real health signal in its current form.** Verification of brand9signs.com requires either (a) an authenticated browser session that already holds the site's sgcaptcha cookie (per `b9-verify-and-index`, direct `curl`/fetch from any cloud sandbox — including this one — gets a CAPTCHA redirect, not real content), or (b) this session's egress proxy allow-listing the domain, which it currently does not. Separately, the check script itself needs to be rewritten against the real static-site structure (answer pages, hub pages, GSC/IndexNow status) — the WooCommerce/Yoast checklist doesn't apply to this site as built today.

## Recommendation

- Re-point this daily check at a session/agent with real browser access to brand9signs.com (Claude-in-Chrome/Hermes path), not this GitHub-repo-scoped session.
- Rewrite the check list to match the actual static-HTML architecture: homepage 200 + hero render, a sample of answer-page URLs (folder-per-slug) returning 200 with expected H1, sitemap presence, OG image on homepage, and any newly-deployed pages in the last 24h — instead of WooCommerce category/product/Yoast checks that don't apply.
