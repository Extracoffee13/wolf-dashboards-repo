# Brand 9 Site Health Report — 2026-08-02

## Overall status: 🟡 YELLOW — Audit could not be executed (environment egress block)

**This is not a verdict on the live health of brand9signs.com.** The scheduled
audit environment's outbound network policy blocked all direct connections to
`brand9signs.com:443` today, so none of the 6 planned checks could be run as
specified. Marking YELLOW rather than RED because there is no evidence of an
actual site outage — only that this session couldn't reach the site directly.
Treat this report as "audit infrastructure failure," not "site is degraded."

## What happened

Every `WebFetch`/`curl` attempt against `https://brand9signs.com/*` returned
HTTP 403 at the CONNECT stage. Checking the agent proxy status endpoint
confirmed this is a policy-level denial, not a response from the site itself:

```
"recentRelayFailures": [
  { "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "brand9signs.com:443" }
]
```

Per the proxy's own guidance, policy denials should be reported rather than
retried or routed around, so no further connection attempts were made.

## Check-by-check status

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | **NOT RUN** — direct fetch blocked |
| 2 | `/product-category/marketing-branding/` — 16-tile nav row + 8 product cards + Motion Films block | **NOT RUN** — direct fetch blocked |
| 3 | 5 random product pages — HTTP 200, image, price | **NOT RUN** — direct fetch blocked |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | **NOT RUN** — direct fetch blocked |
| 5 | OG image for homepage renders | **NOT RUN** — direct fetch blocked |
| 6 | New posts/products published in last 24h | **NOT RUN** — no page access to diff against |

## Indirect signal (WebSearch only, not a live check)

Search-index results confirm the site is indexed and pages resolve in
Google's index as of this run, including:
- `brand9signs.com/product-category/banners/`
- `brand9signs.com/product/marketing-strategy/`
- `brand9signs.com/marketing-services/`
- `brand9signs.com/product/corporate-branding-signage/`
- `brand9signs.com/product/property-branding/`
- `brand9signs.com/product/graphics/`

This is a weak positive signal (Google can reach the site) but says nothing
about console errors, embed rendering, Yoast field values, OG image
correctness, or today's publish activity. None of those can be inferred from
search snippets.

## Recommendation

Re-run this check from an environment/session whose egress policy allows
`brand9signs.com`, or add `brand9signs.com` to this session's allowlist if a
daily automated audit is meant to keep running here. Until then, this daily
job will keep producing YELLOW "audit blocked" reports rather than real
health signal.
