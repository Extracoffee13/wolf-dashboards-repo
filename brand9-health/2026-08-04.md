# Brand 9 Site Health Check — 2026-08-04

## Overall Status: ⚫ BLOCKED (unable to verify)

This is not a RED/YELLOW/GREEN result — the check could not be run at all. Network egress
to `brand9signs.com` was denied by this session's outbound access policy, not by the
target site. No content, timing, or SEO data was observed either way, so no claim about
site health (good or bad) can be made from this run.

## What happened

Every outbound HTTPS attempt to `brand9signs.com` (via WebFetch and via direct `curl`
through the session's egress proxy) failed at the proxy layer with `403` on the CONNECT
tunnel, before any request reached the site:

```
recentRelayFailures:
  kind: connect_rejected
  detail: gateway answered 403 to CONNECT (policy denial or upstream failure)
  host: brand9signs.com:443
```

Per the proxy's own diagnostics guidance, a 403 on CONNECT means "the destination host is
not allowed by your organization's egress policy for this session" — explicitly not
something to retry or route around. `WebSearch` for `brand9signs.com` also returned no
indexed results for the domain, so no secondary signal was available either.

## Checklist status (all unrun)

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | Not run — egress blocked |
| 2 | Marketing/Branding category page: 16-tile nav + 8 product cards + Motion Films block | Not run — egress blocked |
| 3 | 5 random product pages: HTTP 200, image, price | Not run — egress blocked |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | Not run — egress blocked |
| 5 | OG image renders for homepage URL | Not run — egress blocked |
| 6 | New posts/products in last 24h | Not run — egress blocked |

## Recommendation

This session's network policy does not currently allow outbound access to
`brand9signs.com`. To restore this daily check, the environment/session network policy
needs to allow-list that host (or the check needs to run from an environment configured
with browsing access to the live site). No action is needed on the WordPress site itself
based on this run — there is simply no data.
