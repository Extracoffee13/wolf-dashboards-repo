# Brand 9 Site Health Report — 2026-08-01

## Overall status: 🟡 YELLOW — audit inconclusive (network policy block, not a confirmed site issue)

This session's outbound HTTPS proxy denied the CONNECT to `brand9signs.com:443`
with a policy-level 403 (`gateway answered 403 to CONNECT (policy denial or
upstream failure)`), confirmed via both `WebFetch` and a direct `curl` from
this environment, and corroborated by the proxy status endpoint
(`recentRelayFailures` showing two `connect_rejected` entries for
`brand9signs.com:443` at 16:03:50 UTC). Per this environment's proxy runbook,
403/407 from the gateway means the destination host is not allowed by the
org's egress policy for this session — it is not something to retry or route
around, and it is not evidence the live site is down. As a result, the six
checks below could not be run directly against the site this run. Google's
index (queried via WebSearch, which does not go through the blocked proxy
path) does show recently-crawled pages resolving under brand9signs.com,
which is a weak positive signal the site is reachable from the open
internet — but it does not substitute for the requested direct checks.

**Action needed:** allowlist `brand9signs.com` for this environment's egress
policy (or run this check from a session/tool that has that access) so
tomorrow's run can perform the actual checks.

## Checklist (planned vs. actual)

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⛔ Not run — direct fetch blocked |
| 2 | `/product-category/marketing-branding/` shows 16-tile nav row + 8 product cards + Motion Films block | ⛔ Not run — direct fetch blocked |
| 3 | 5 random product pages: HTTP 200 + image + price | ⛔ Not run — direct fetch blocked |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase/meta description/SEO title, no placeholder leakage) | ⛔ Not run — direct fetch blocked |
| 5 | OG image renders for homepage | ⛔ Not run — direct fetch blocked |
| 6 | New posts/products in last 24h | ⛔ Not run — direct fetch blocked |

## Indirect signal (via WebSearch only)

- Search-indexed pages under brand9signs.com resolve and are being crawled,
  including `/product/marketing-strategy/`, `/product/graphics/`,
  `/product/corporate-branding-signage/`, `/product/corporate-wall-branding/`,
  `/marketing-services/`, `/about-us-brand-9/`, `/services-brand-9/`.
- No specific Motion Films *product* page URL was surfaced in search results
  this run (searches returned the general "Motion Graphics" service
  description, not a dedicated `/product/motion-films/`-style page) — worth
  confirming manually since check #4 depends on locating that exact page.
- No signal either way on new posts/products published in the last 24h
  (check #6) — search snapshots lag real-time changes and can't confirm this.

## Recommendation

Treat this as an infrastructure gap in the monitoring pipeline, not a site
health finding. Re-run once `brand9signs.com` is reachable from this
session, or move the check to a session/agent with a permissive egress
policy for this domain.
