# Brand 9 Site Health Check — 2026-07-24

## Overall status: 🔴 RED — audit could not run

The scheduled 6-step health audit against `brand9signs.com` could **not be executed**. Every outbound request to the domain (via WebFetch and via direct `curl` through the sandbox's egress proxy) was rejected at the network layer before reaching the site.

## What happened

- `WebFetch` on `https://brand9signs.com` → HTTP 403 (no response body).
- `WebFetch` on `https://brand9signs.com/product-category/marketing-branding/` → HTTP 403 (no response body).
- Direct `curl` to `https://brand9signs.com/` → `curl: (56) CONNECT tunnel failed, response 403` (proxy-level rejection, not a site response).
- Proxy status endpoint (`$HTTPS_PROXY/__agentproxy/status`) confirmed the rejection is a **policy denial at the egress gateway**, not a site-side error:
  ```
  "kind": "connect_rejected",
  "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
  "host": "brand9signs.com:443"
  ```
  This occurred on two independent attempts (homepage and category page), both logged in `recentRelayFailures`.
- `WebSearch` for `brand9signs.com` returned no indexed results for the domain — no independent signal was available to substitute for direct checks.

Per the environment's own guidance (`/root/.ccr/README.md`): 403 responses from the proxy indicate the destination host is not allowed by the current session's organizational egress policy, and should be **reported, not retried or routed around**. No further retries were attempted.

## Checklist status (all blocked — none could be verified)

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⛔ Not verified — network blocked |
| 2 | Marketing/Branding category page: 16-tile nav row + 8 product cards + Motion Films block | ⛔ Not verified — network blocked |
| 3 | 5 random product pages: HTTP 200, image, price | ⛔ Not verified — network blocked |
| 4 | Yoast SEO meta on Motion Films product page | ⛔ Not verified — network blocked |
| 5 | OG image for homepage | ⛔ Not verified — network blocked |
| 6 | New posts/products in last 24h | ⛔ Not verified — network blocked |

## Headline finding

This is **not evidence of a site outage** — it is evidence that this particular scheduled-run session's network egress policy does not currently permit reaching `brand9signs.com`. The site itself may be perfectly healthy; the audit simply has no visibility into it today. This should be flagged to whoever owns the environment/egress configuration for this scheduled task, since a health check that silently can't reach its target is worse than no health check at all — it will report nothing while looking like a routine no-op.

## Recommended next step

Confirm `brand9signs.com` (and its wp-content asset host, if different) is on the egress allowlist for whatever environment/session profile runs this daily schedule. Once allowlisted, re-run this check.
