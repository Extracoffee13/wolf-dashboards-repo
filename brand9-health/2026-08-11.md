# Brand 9 Site Health Check — 2026-08-11

## Overall status: 🔴 RED

The scheduled 6-step audit could not be completed as specified. Two independent
blockers were hit, and they compound each other:

1. **Tooling blocker:** `WebFetch` is fully blocked by this session's network
   egress proxy for every domain tested, not just brand9signs.com (confirmed
   by also testing `www.anthropic.com`, same `EGRESS_BLOCKED` error). This
   cloud/remote session has no route to fetch live page HTML, console errors,
   image/price rendering, Yoast meta fields, or OG image bytes. `WebSearch`
   still works and was used to gather what indirect signal it could, but
   search snippets cannot confirm HTTP status, console errors, load time, or
   meta-tag population — none of the 6 requested checks can be certified this
   way.

2. **Spec-mismatch blocker (more concerning):** the checks in this task
   describe a WooCommerce-style catalog — a `/product-category/marketing-branding/`
   page with a 16-tile nav row, 8 product cards, a "Motion Films" feature
   block, a "Motion Films" product page with Yoast SEO fields, and a homepage
   "motion film embed." Neither this repo's `b9-site-deploy` operating notes
   nor a `WebSearch` sweep of `site:brand9signs.com` turn up any trace of a
   "Motion Films" product or a "marketing-branding" category:
   - Real product-category slugs found: `wall-signs-3d`, `architectural-products`,
     `environmental-graphic-design`, `school-branding` (plus, per prior notes,
     monument signs, government agencies, corporate branding, property
     branding, branded environments, church branding, banners).
   - `site:brand9signs.com "marketing-branding"` returns no matching page —
     closest hits are `/brand-lab-ai/`, `/branding-vs-marketing-2024/`, and
     `/product/marketing-strategy/`, none of which match the described layout.
   - No search result anywhere ties "Motion Films" to brand9signs.com.
   - This repo's own `b9-site-deploy` skill notes (verified 2026-08-08) state
     brand9signs.com is a **static HTML build on DreamHost — explicitly NOT
     WordPress** (`/wp-json/` returns the static site's custom 404). Yoast SEO
     is a WordPress plugin; if that note is current, a Yoast meta check on
     this domain is checking for something that structurally cannot exist.

Given (1) no live verification was possible and (2) the target content
described doesn't match the site's known real structure, this is reported RED
rather than a false GREEN — the honest state is "could not verify, and the
brief itself may be stale or pointed at the wrong site/section."

## Per-step results

| # | Check | Result |
|---|---|---|
| 1 | Homepage loads, no console errors, motion film embed renders | **Not verifiable** — WebFetch blocked; no console access from this tool regardless |
| 2 | `/product-category/marketing-branding/` — 16-tile nav + 8 product cards + Motion Films block | **Not found** — no such page surfaced in search; category doesn't appear to exist |
| 3 | 5 random product pages — HTTP 200, image, price | **Not verifiable** — WebFetch blocked |
| 4 | Yoast SEO meta on Motion Films product page | **Not verifiable / likely N/A** — no "Motion Films" product found; site is reportedly non-WordPress |
| 5 | OG image for homepage renders | **Not verifiable** — WebFetch blocked |
| 6 | New posts/products in last 24h | **Not verifiable** — no fetch access; nothing surfaced via search |

## Recommendation

- Re-run this check from a session with live browser access to brand9signs.com
  (Claude-in-Chrome / Cowork, which holds the sgcaptcha-authenticated session
  per this repo's `b9-verify-and-index` notes) rather than this cloud session,
  which has no route to the domain at all.
- Before the next run, confirm with Bobby/AP whether "Motion Films," the
  "Marketing/Branding" category, and Yoast SEO are current brand9signs.com
  features — or whether this check template was written for a different
  site/era (e.g. a pre-rebuild WooCommerce store) and needs updating to match
  the current static-build product-category taxonomy.
