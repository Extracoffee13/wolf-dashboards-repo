# Brand 9 Signs — Daily Site Health Check
**Date:** 2026-07-01
**Target:** https://brand9signs.com
**Overall status:** 🔴 RED — audit could not be completed (blocked by sandbox network policy, not a confirmed site failure)

## Blocker (read first)

Every direct fetch to `brand9signs.com` from this session was rejected by the
execution environment's outbound network policy, **not** by the target site:

```
gateway answered 403 to CONNECT (policy denial or upstream failure)
host: brand9signs.com:443
```

This was reproduced 3 times (homepage, `/product-category/marketing-branding/`,
`/sitemap.xml`) via the `WebFetch` tool, and confirmed as a policy-level block
(not a transient error) via the agent proxy status endpoint. A raw `curl` to
an unrelated control domain (`example.com`) was **also** rejected identically,
confirming this session has no general outbound HTTPS egress — only an
allowlisted set of infra domains (npm, PyPI, GitHub, anthropic.com, etc.) is
reachable directly. Per the environment's own guidance, policy denials should
be reported rather than retried/routed around, so no further fetch attempts
were made.

**Net effect:** checks 1, 2, 3, 4, and 5 below (all of which require an
authenticated live fetch — status codes, rendered embeds, console state,
meta-tag values, image rendering) **could not be performed** this run. The
findings below for those items are marked accordingly. `WebSearch` (Anthropic's
own search backend, not a direct site fetch) remained available and was used
to gather secondary, index-based signal only — it cannot substitute for a
live check.

## Check results

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⚠️ **NOT VERIFIED** — direct fetch blocked |
| 2 | `/product-category/marketing-branding/` — 16-tile nav + 8 product cards + Motion Films block | ⚠️ **NOT VERIFIED** — direct fetch blocked |
| 3 | 5 random product pages — HTTP 200 + image + price | ⚠️ **NOT VERIFIED** — direct fetch blocked |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase/description/title, no placeholder leakage) | ⚠️ **NOT VERIFIED** — direct fetch blocked; a dedicated "Motion Films" product URL was not even surfaced via search index (see below) |
| 5 | OG image for homepage renders | ⚠️ **NOT VERIFIED** — direct fetch blocked |
| 6 | New posts/products in last 24h | ⚠️ **PARTIAL** — see below |

### Item 6 detail (via WebSearch, index-based only)

Search-index snapshots show a blog/news hub at `brand9signs.com/blog-news/`
with most recently *indexed* content dated around **June 11, 2026**. No
content specifically dated in the last 24h (June 30–July 1, 2026) surfaced in
search results. This is **not equivalent** to checking the live WP admin/post
list — search indexes lag and may miss same-day publishes entirely. Treat as
inconclusive, not as confirmation nothing new was published.

### Item 4 detail (Motion Films page)

A dedicated "Motion Films" product page was not returned by search queries
against `brand9signs.com` (searches did surface `/product/graphics/`,
`/product/corporate-branding-signage/`, `/product/branded-environments/`,
`/product/home-signs/`, `/product/property-branding/`,
`/product/brand-identity-signs/`, and the `/product-category/marketing-branding/`
archive, which the task description says should have a "Motion Films feature
block" beneath it). Whether this is a search-index gap or a real absence of
the page cannot be determined without a live fetch.

## Recommendation

This is an infrastructure/tooling gap in the current session, not a verdict
on Brand 9's site. To get an actual GREEN/YELLOW/RED verdict tomorrow, either:
- run this check from an environment whose network policy allowlists
  `brand9signs.com`, or
- provide an authenticated/allowlisted fetch path (e.g., a monitoring
  webhook, an MCP tool with its own egress) that isn't subject to this
  session's egress policy.

Flagging as 🔴 RED so a human confirms real site health rather than trusting
a stale/incomplete audit.
