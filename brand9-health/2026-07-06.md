# Brand 9 Site Health Check — 2026-07-06

## Overall status: 🔴 RED — check could not be executed

The scheduled audit could not reach `brand9signs.com`. All outbound fetch
attempts (WebFetch and raw `curl` through the session's egress proxy) were
rejected at the proxy layer with `HTTP 403` on the CONNECT tunnel itself —
this is an organization network-policy denial, not a response from the
Brand 9 site. Per the proxy's own guidance, policy denials should be
reported rather than retried or routed around, so no live checks could be
completed this run.

Confirmed via `/root/.ccr/__agentproxy/status`:
```
kind: connect_rejected
detail: gateway answered 403 to CONNECT (policy denial or upstream failure)
host: brand9signs.com:443
```

Three distinct URLs were attempted (homepage, `/product-category/marketing-branding/`,
a product page on `www.brand9signs.com`) — all blocked identically, confirming
this is a domain-wide egress block for this session, not a path- or
subdomain-specific issue.

## Checks — status

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⛔ Not run — network blocked |
| 2 | Marketing/Branding category page: 16-tile nav + 8 product cards + Motion Films block | ⛔ Not run — network blocked |
| 3 | 5 random product pages: HTTP 200 + image + price | ⛔ Not run — network blocked |
| 4 | Yoast SEO meta on Motion Films product page | ⛔ Not run — network blocked |
| 5 | OG image for homepage renders | ⛔ Not run — network blocked |
| 6 | New posts/products in last 24h | ⛔ Not run — network blocked |

## What WebSearch (indirect, cached-index) could confirm

WebSearch (a separate tool path not subject to the same per-host proxy
block) confirms the following pages are indexed and exist:
- `https://brand9signs.com/` — homepage, title "Brand 9 Signs | Homebuilder,
  Military & Development Signage"
- `https://brand9signs.com/product-category/marketing-branding/` — "Marketing
  & Branding Archives | Brand 9 Signs"
- Several `/product/*` pages (state park signs, custom 3D signs, brand
  identity signs, classic monument signs, cabinet signs, etc.)

This only confirms the pages are indexed by search engines as of their last
crawl — it says nothing about current HTTP status, live rendering, console
errors, embed behavior, Yoast meta fields, or OG image correctness. None of
the six required checks can be considered verified from this alone.

## Recommendation

This session's environment needs `brand9signs.com` (and `www.brand9signs.com`)
added to its outbound network allowlist for this check to run as designed.
Until that's in place, this daily routine will keep coming back RED for
"could not run" rather than reporting genuine site health.
