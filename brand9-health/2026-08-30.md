# Brand 9 Site Health Report — 2026-08-30

## Overall status: 🔴 RED (audit could not be executed)

This run could not perform the requested checks and is flagging that fact rather than
fabricating results. Two independent blockers, both confirmed directly in this session:

## Blocker 1 — no network path to brand9signs.com from this session

`WebFetch` to `https://brand9signs.com` and `https://brand9signs.com/product-category/marketing-branding/`
both returned `EGRESS_BLOCKED — Access to brand9signs.com is blocked by the network egress proxy`
before any HTTP request left the sandbox. This session has no `WebFetch`/`curl` path to the domain
at all, and no browser-automation tool (Claude-in-Chrome / Hermes `computer_*`) is available here
either — only `WebFetch`/`WebSearch` and the git repo. None of steps 1–5 in the requested checklist
(homepage load, category page layout, 5 product pages, Yoast meta, OG image) could be run this way.

Separately, per this repo's own `b9-verify-and-index` skill, brand9signs.com sits behind an
`sgcaptcha` bot-shield: even when egress *is* open (e.g. from Hermes or a plain cloud `curl`), a
non-browser fetch gets a 202 CAPTCHA interstitial, not the real page. The only verified-working
path is an authenticated Chrome session (Claude-in-Chrome or Hermes CDP) holding the shield's
cookie — not available in this scheduled session.

## Blocker 2 — the requested checklist doesn't match the site that exists

Independent of the egress block, this repo's own `b9-website-architecture` skill and a live
`WebSearch` against the domain both show brand9signs.com is **Brand 9 Signs**, a Jacksonville/
Orange Park, FL commercial signage fabricator (dimensional letters, wayfinding, donor walls,
community/homebuilder signage) — a **static HTML rebuild**, not WooCommerce/WordPress-served
content. Concretely:

- There is no `/product-category/marketing-branding/` taxonomy page, no "16-tile nav row + 8
  product cards + Motion Films feature block" layout — the site's category pages live at
  `/product/<slug>/` (one service page per category, e.g. `/product/development-signage/`) and the
  hub is `/portfolio/` (accordion of ~24 categories), not a WooCommerce shop grid.
- There is no "Motion Films product page" to check Yoast focus keyphrase / meta description / SEO
  title on. A real WordPress+Yoast install still exists at the old docroot, but per the
  architecture skill **it renders none of the live pages** — WP admin/Yoast edits do nothing
  visitors ever see, so a Yoast check would be auditing a dead backend, not the live site.
  WebSearch results (below) show real live pages are signage-service pages like
  `/sign-company-orange-park-fl/`, `/jacksonville-sign-company/`, `/the-evolution-of-brand-9-signs/`
  — nothing matching "Motion Films" as a product.
- No evidence of "5 random product pages" as an e-commerce concept — the closest analog is the
  ~95 `/product/<slug>/` service pages, which is a different content model (no price, no cart).

This checklist reads like it was written for a different site (a WooCommerce storefront with a
"Marketing/Branding" category and a "Motion Films" product+price), not for brand9signs.com as it
actually exists today. It's possible the prompt is stale from before the 2026-08-04/05 WordPress→
static migration, or was drafted for a different property entirely.

## What this run did confirm

- `brand9signs.com` resolves and is publicly indexed — WebSearch returned live, plausible-looking
  URLs (`/sign-company-orange-park-fl/`, `/jacksonville-sign-company/`,
  `/the-evolution-of-brand-9-signs/`), consistent with the documented static-site architecture.
- No new posts/products could be checked for the last-24h window (step 6) — same blockers as above.

## Recommendation

1. Route this daily check through a session with Claude-in-Chrome/Hermes access (per
   `b9-site-health-sentinel`), not plain `WebFetch`/`WebSearch` — this session's tools cannot get
   past the egress block or the sgcaptcha shield.
2. Re-scope the checklist to the real page inventory: homepage, `/portfolio/` hub, a sample of
   `/product/<slug>/` pages, `sitemap.xml`, and drop the WooCommerce/Yoast-product-page assumptions
   unless a distinct e-commerce property is actually intended.
3. If "Motion Films" is a real, separate asset (e.g. a homepage embed), point the next run at its
   actual URL/slug rather than a WordPress product page.

No commit to site code was made or needed — this was a read-only audit attempt.
