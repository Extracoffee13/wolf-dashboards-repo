# Brand 9 Site Health Report — 2026-07-15

## Overall Status: 🔴 RED (monitoring blocked — audit could not complete)

**Headline finding:** This session's outbound network policy blocked all direct HTTPS fetches (WebFetch and raw curl), so the six required live checks (console errors, tile/card counts, HTTP status codes, image/price rendering, Yoast meta inspection, OG image render) could not be executed against brand9signs.com this run. Status is reported RED as a fail-closed default because critical checks are unverified, **not** because a live defect was confirmed on the site itself.

---

## What happened

- `WebFetch` on `https://brand9signs.com` and `https://brand9signs.com/product-category/marketing-branding/` both returned **HTTP 403 Forbidden** from the outbound gateway (not from the target site).
- `curl` via the environment's HTTPS proxy failed identically with `CONNECT tunnel failed, response 403`.
- Confirmed this is an environment-wide network policy restriction, not something specific to brand9signs.com: the same 403 occurred fetching `https://example.com`, `https://www.google.com`, and `https://wordpress.org`.
- The proxy status endpoint (`$HTTPS_PROXY/__agentproxy/status`) logged the rejection explicitly:
  ```
  "kind": "connect_rejected",
  "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
  "host": "brand9signs.com:443"
  ```
- This session's environment does not have outbound web access enabled for this domain (or generally). WebSearch (a separate, non-proxied tool) still worked and was used as a best-effort fallback signal only.

## Checklist results

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⚠️ NOT VERIFIED — could not fetch/render page |
| 2 | Marketing/Branding category page: 16-tile nav row + 8 product cards + Motion Films feature block | ⚠️ NOT VERIFIED — page confirmed to exist via search index (URL indexed, title "Marketing & Branding Archives \| Brand 9 Signs"), but tile/card counts and feature block content could not be inspected |
| 3 | 5 random product pages: HTTP 200 + image + price | ⚠️ NOT VERIFIED — no HTTP status checks possible this run |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | ⚠️ NOT VERIFIED — could not load page source/admin meta; a dedicated "Motion Films" product page was not clearly located even via search (only general motion-graphics service copy surfaced) |
| 5 | OG image for homepage renders | ⚠️ NOT VERIFIED |
| 6 | New posts/products in last 24h | ⚠️ NOT VERIFIED via crawl; no obvious new content surfaced in search snippets, but search indexing lag makes this an unreliable signal for a 24h window |

## Best-effort fallback signal (WebSearch only)

Using `site:brand9signs.com` searches, the following pages are indexed and resolving normally as of this run, which is a weak positive signal the site is generally up:
- Homepage — https://brand9signs.com/
- Marketing & Branding category — https://brand9signs.com/product-category/marketing-branding/
- Several product pages (Corporate Wall Branding, Home Builder Marketing, Corporate Branding Signage, Monument Signs, Dimensional Sign Letters, Custom Restaurant Signs, Graphics)
- About, Services, Contact pages

This is **not** a substitute for the live checks above — it does not confirm console errors, image/price rendering, HTTP status per page, or Yoast field state.

## Recommendation

Re-run this check from a session/environment with outbound web access enabled for brand9signs.com (or route health checks through an MCP tool with an allowlisted egress path). Until then, treat this automated health check as non-functional rather than "site is healthy."
