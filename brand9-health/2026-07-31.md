# Brand 9 Site Health Check — 2026-07-31

## Overall status: ⚫ BLOCKED (unable to run checks)

This run could not reach brand9signs.com at all. The session's outbound
network egress proxy rejected every `CONNECT` to `brand9signs.com:443` with
a 403 at the gateway level — logged by the proxy as
`connect_rejected: gateway answered 403 to CONNECT (policy denial or
upstream failure)`. This is an organization egress-policy denial for this
host, not an error returned by the WordPress site itself, and per the
environment's own guidance such denials must be reported rather than
retried or routed around.

Confirmed via three independent attempts, all failing identically:
- `WebFetch https://brand9signs.com` → HTTP 403 (proxy-level)
- `WebFetch https://brand9signs.com/product-category/marketing-branding/` → HTTP 403 (proxy-level)
- `WebFetch https://brand9signs.com/privacy-policy/` → HTTP 403 (proxy-level)
- Raw `curl` to the domain → `CONNECT tunnel failed, response 403`

`WebSearch` (which does not route through this proxy) still resolved the
domain and returned indexed pages (homepage, `/privacy-policy/`,
`/brand-lab-ai/`), so the site itself is up and indexed by search engines —
this is specifically an outbound-fetch restriction on this session, not a
site outage.

## Checklist — not run (blocked before any check could execute)

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | NOT RUN — fetch blocked |
| 2 | Marketing/Branding category page: 16-tile nav + 8 product cards + Motion Films block | NOT RUN — fetch blocked |
| 3 | 5 random product pages: HTTP 200 + image + price | NOT RUN — fetch blocked |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | NOT RUN — fetch blocked |
| 5 | Homepage OG image renders | NOT RUN — fetch blocked |
| 6 | New posts/products in last 24h | NOT RUN — fetch blocked (WebSearch found no new indexed content, but this is not a reliable substitute for a direct check) |

## What this means

No content or SEO conclusions can be drawn from this run — good or bad.
The status above reflects a tooling/access failure in this session's
environment, not a finding about brand9signs.com. This should be reported
so the network egress policy for this environment/session can be reviewed
(brand9signs.com added to the allowlist, or an alternate fetch path
configured) so tomorrow's run can actually check the site.

## Next steps

- Confirm brand9signs.com is on the allowed egress list for this
  environment's network policy, or route this check through a tool/session
  that has direct access.
- Re-run the full 6-point checklist once access is restored.
