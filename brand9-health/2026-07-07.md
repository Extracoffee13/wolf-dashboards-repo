# Brand 9 Signs — Daily Site Health Check
**Date:** 2026-07-07
**Target:** https://brand9signs.com
**Overall status: 🔴 RED — check could not be completed (network egress blocked)**

## Blocker

Every direct fetch to `brand9signs.com` (homepage, category page, product pages) was rejected before reaching the server:

- `WebFetch` → `HTTP 403 Forbidden` (no response body)
- `curl` (direct and through the session's agent proxy) → `403` / connection refused
- Agent proxy status (`/root/.ccr/__agentproxy/status`) confirms the block is at the egress gateway, not the target site:
  ```
  "kind": "connect_rejected",
  "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
  "host": "brand9signs.com:443"
  ```
  This fired identically for the homepage, the marketing/branding category page, and a product page — a domain-wide policy denial, not a one-off site error.

Per the proxy's own guidance, policy denials of this kind should be reported rather than retried, so no further fetch attempts were made this run.

## What that means for the 6 checks

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⛔ Not verifiable — direct fetch blocked |
| 2 | Marketing/Branding category page (16-tile nav + 8 product cards + Motion Films block) | ⛔ Not verifiable — direct fetch blocked |
| 3 | 5 random product pages: HTTP 200 + image + price | ⛔ Not verifiable — direct fetch blocked |
| 4 | Yoast SEO meta on Motion Films product page | ⛔ Not verifiable — direct fetch blocked |
| 5 | OG image renders for homepage URL | ⛔ Not verifiable — direct fetch blocked |
| 6 | New posts/products in last 24h | ⛔ Not verifiable — direct fetch blocked |

## Indirect signal (WebSearch only)

A `site:brand9signs.com` web search still returns and indexes the domain normally (homepage, store page, several product/category pages, a Jacksonville location page), which suggests the site itself is generally reachable to search crawlers and not fully down. This is **not** a substitute for the live checks above — it can't confirm console errors, embed rendering, Yoast field population, live pricing, or content published in the last 24 hours.

## Action needed

This session's outbound network policy does not currently permit requests to `brand9signs.com`. To restore this daily check, the environment's egress allowlist needs to include that host (see the Claude Code on the web docs for environment network policy configuration). Until then, this automated health check cannot verify live site behavior and should be treated as unverified/RED rather than assumed healthy.
