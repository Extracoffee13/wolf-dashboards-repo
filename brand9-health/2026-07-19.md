# Brand 9 Site Health Check — 2026-07-19

## Overall status: 🟡 YELLOW — checks incomplete (session egress blocked, not a confirmed site failure)

This run could **not** directly fetch brand9signs.com. Both `WebFetch` and a raw
`curl` through the session's egress proxy returned `403`. The proxy's own
status log confirms this is a policy-level denial, not a site-side error:

```
kind: connect_rejected
detail: gateway answered 403 to CONNECT (policy denial or upstream failure)
host: brand9signs.com:443
```

Per the proxy runbook, 403 policy denials should be reported, not retried or
worked around. As a fallback, WebSearch (which hits a search API, not the
site directly) was used to cross-check that the domain is live and indexed.
That is a much weaker signal than a real fetch — it cannot see console
errors, embed rendering, HTTP status of individual pages, Yoast meta fields,
or OG image rendering. Items below are marked accordingly.

## Checklist

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⚠️ NOT VERIFIED — direct fetch blocked (403 via proxy). WebSearch confirms homepage is indexed and reachable to crawlers ("Homebuilder Signage Florida \| Sign Company \| Brand 9 Signs"), no error content surfaced. Console/embed rendering unobservable without a live fetch or browser. |
| 2 | `/product-category/marketing-branding/` shows 16-tile nav row + 8 product cards + Motion Films block | ⚠️ NOT VERIFIED — direct fetch blocked. WebSearch confirms the page is indexed ("Marketing & Branding Archives \| Brand 9 Signs") and surfaces category-adjacent products (Corporate Wall Branding, Home Builder Marketing, Corporate Branding Signage, Monument Signs). Exact tile/card counts and the Motion Films block cannot be confirmed without rendering the page — a dedicated "Motion Films" page/section did not surface in search at all, which is worth a manual look. |
| 3 | 5 random product pages: HTTP 200, image, price | ⚠️ NOT VERIFIED — no way to check per-page HTTP status or render images/prices without a live fetch. Product URLs are indexed and resolving in search (e.g. `/product/corporate-wall-branding/`, `/product/monument-signs/`, `/product/dimensional-sign-letters/`), which is a weak positive signal only. |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no "yoast-placeholder" leakage) | ⚠️ NOT VERIFIED — requires page fetch/HTML inspection. Additionally, no page titled or URLed as "Motion Films" turned up in search at all (searches only returned general "motion graphics" service mentions) — can't confirm this page currently exists at a stable URL. Needs manual confirmation of the correct URL before Yoast fields can be checked. |
| 5 | OG image for homepage renders | ⚠️ NOT VERIFIED — requires direct fetch of the page/og:image URL. |
| 6 | New posts/products published in last 24h | ⚠️ NOT VERIFIED — no sitemap/date-filtered crawl available via search; nothing in results pointed to a same-day publish. |

## Root cause

The session's outbound HTTPS proxy denied the CONNECT to `brand9signs.com:443`
with a 403 (`policy denial or upstream failure`) on every attempt (WebFetch
tool and raw curl). This blocks all direct verification for this scheduled
run. It is an environment/session network-policy issue, not evidence of a
problem with the live site.

## Recommendation

- Confirm/adjust the egress allowlist for this environment so
  `brand9signs.com` is reachable for future scheduled health checks.
- Manually confirm the correct URL for the "Motion Films" product page —
  it did not surface under that name in any search query this run.
- Once fetch access is restored, re-run this checklist in full before
  trusting a GREEN/RED status from this report.
