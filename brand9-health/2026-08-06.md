# Brand 9 Site Health Report — 2026-08-06

## Overall status: 🔴 RED

**Headline finding:** The automated health checks could not be executed. This session's outbound network egress policy blocks all direct connections to `brand9signs.com` (WebFetch and curl both fail with a policy-level 403 at the proxy's CONNECT stage, confirmed via the proxy diagnostic endpoint — this is an organization egress denial, not a response from the website itself). Per the proxy's own guidance, blocked hosts must be reported, not routed around, so items 1–5 below are **unverified today**, not passing.

---

## 1. Homepage load / motion film embed
**Status: UNVERIFIED (blocked)** — Could not fetch `https://brand9signs.com` directly. No console-error or embed-render data available.

## 2. Marketing/Branding category page layout
**Status: UNVERIFIED (blocked)** — Could not fetch `/product-category/marketing-branding/`. Tile row, product card count, and Motion Films feature block could not be confirmed.

## 3. Five random product pages (HTTP 200 + image + price)
**Status: UNVERIFIED (blocked)** — No direct page fetch was possible; HTTP status, product images, and prices could not be checked.

## 4. Yoast SEO meta on Motion Films product page
**Status: UNVERIFIED (blocked)** — Focus keyphrase, meta description, and SEO title could not be inspected directly.

## 5. OG image for homepage URL
**Status: UNVERIFIED (blocked)** — Could not retrieve or render the OG image asset.

## 6. New posts/products in last 24h
**Status: PARTIAL, via WebSearch only** — WebSearch (which is not subject to the same egress block) confirms the site is normally indexed and reachable to search crawlers: known pages such as `/portfolio/`, `/about-us-brand-9/`, `/product/corporate-branding-signage/`, `/product/graphics/`, and `/product/marketing-strategy/` all return normal-looking indexed snippets, with no signs of a global outage. This is a weak, indirect signal only — it does not confirm freshness of any specific post/product in the last 24 hours, and does not substitute for a direct fetch.

---

## Diagnostic detail

Proxy status endpoint (`$HTTPS_PROXY/__agentproxy/status`) recorded four consecutive `connect_rejected` entries for `brand9signs.com:443` at 2026-08-06T16:15:44–51Z, each with detail `"gateway answered 403 to CONNECT (policy denial or upstream failure)"`. A direct `curl -sI` (bypassing WebFetch) hit the same 403 at the CONNECT tunnel stage — before ever reaching brand9signs.com's own server — confirming this is an egress allowlist gap for this session/environment, not a WordPress/Yoast/hosting issue.

## Recommendation

Add `brand9signs.com` to this environment's egress allowlist (or run this check from an environment/session where it's already allowed) so tomorrow's run can perform the real content, embed, Yoast, and OG-image checks this report is supposed to deliver.
