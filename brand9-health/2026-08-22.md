# Brand 9 Site Health Report — 2026-08-22

## Overall status: 🔴 RED (audit could not run)

The scheduled 6-step live audit of brand9signs.com could not be executed from this
session. This is a tooling/access failure, not a clean bill of health — the site's
actual state is unverified today.

## Why it failed

1. **`web_fetch` to brand9signs.com is hard-blocked at the network egress proxy**
   in this environment (`EGRESS_BLOCKED` on every attempt, both the homepage and
   the marketing-branding category page). No live HTML, headers, status codes, or
   render state could be retrieved.
2. Independent of that block, brand9signs.com sits behind a bot/challenge layer:
   plain HTTP fetches (curl/urllib-style, which is what `web_fetch` is under the
   hood) reportedly get a ~181-byte interstitial (HTTP 202) instead of the real
   page — confirmed by prior site-health tooling notes in this workspace. A real
   check needs an authenticated/real browser session (Hermes CDP or an already
   logged-in Chrome tab), which this session does not have.
3. `WebSearch` (Google's index, not a live check) was used as a fallback and
   confirms the pages exist in the index — `/product-category/marketing-branding/`,
   `/product/marketing-strategy/`, `/product/corporate-branding-signage/`, etc. —
   but indexed snapshots cannot confirm today's HTTP status, console errors, load
   time, embed rendering, or current Yoast/meta field values. Treat everything
   below as "known to exist as of last crawl," not "verified healthy today."

## Step-by-step outcome

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | **NOT VERIFIED** — no live fetch possible |
| 2 | `/product-category/marketing-branding/` shows 16-tile nav + 8 product cards + Motion Films block | **NOT VERIFIED** — page exists in Google's index, layout/content unconfirmed |
| 3 | 5 random product pages: HTTP 200 + image + price | **NOT VERIFIED** — no status codes retrievable |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | **NOT VERIFIED**. Also flagging a possible premise mismatch: this workspace's `b9-site-deploy` skill (verified 2026-08-08) states brand9signs.com's core build is a **static HTML site, not WordPress**, and explicitly warns off Yoast-oriented workflows as describing a "dead legacy WordPress backend." However, WebSearch does show live `/product/` and `/product-category/` URLs in the index, which look WooCommerce-shaped — so the site may now have a WordPress/WooCommerce storefront layered in since that skill was last verified, or the skill's static-site claim may only cover the answer-page section. This should be reconciled with a real browser check before trusting either source. |
| 5 | OG image renders for homepage URL | **NOT VERIFIED** |
| 6 | New posts/products published in last 24h | **NOT VERIFIED** — no changelog/feed access from this session |

## Recommendation

Re-run this audit through a path that actually has browser access to the live
site — e.g. the `b9-site-health-sentinel` flow (Chrome/Hermes CDP + injected
collector script), which is the documented working method for this domain given
its bot-challenge. A `web_fetch`-only session (like this one) cannot produce a
trustworthy report for brand9signs.com and should not be scheduled to do so
without that browser path available.

Separately, someone should reconcile whether brand9signs.com is currently a pure
static build or has a live WooCommerce product catalog — the two internal
sources disagree, and that ambiguity should be resolved before the next Yoast
check is treated as meaningful.
