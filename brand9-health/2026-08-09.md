# Brand 9 Site Health Check — 2026-08-09

## Overall status: 🔴 RED — check could not be executed

## Summary

This session's network egress policy blocks direct access to `brand9signs.com`
(WebFetch returned `EGRESS_BLOCKED` for both the homepage and the
Marketing/Branding category page). Per this environment's proxy guidance
("do not retry or route around organization policy denials"), no live fetch
was retried or bypassed. As a result, none of the 6 requested checks could be
performed against the live site in this run:

1. Homepage load / console errors / motion film embed — **not checked** (egress blocked)
2. `/product-category/marketing-branding/` tile row + 8 product cards + Motion Films block — **not checked** (egress blocked)
3. 5 random product pages (HTTP 200 + image + price) — **not checked** (egress blocked)
4. Yoast SEO meta on Motion Films product page — **not checked** (egress blocked)
5. OG image render for homepage — **not checked** (egress blocked)
6. New posts/products in last 24h — **not checked** (egress blocked)

## What was possible

A `WebSearch` query for `brand9signs.com` returned indexed results confirming
the domain resolves and has recently-crawled pages (e.g.
`brand9signs.com/brand-lab-ai/`, `brand9signs.com/sign-company-orange-park-fl/`),
but search-index snapshots are not a substitute for a live HTTP/console check
and were not used to infer pass/fail status on any of the 6 items above.

## Root cause

This Claude Code session is scoped to the GitHub repo
`extracoffee13/wolf-dashboards-repo` only, running in a container whose
egress proxy allow-list does not include `brand9signs.com`. This looks like
an environment/scheduling mismatch: the health-check task expects live
fetch access to the Brand 9 site, but the environment this scheduled task
fired in does not grant it.

## Recommendation

Run this daily check from a session/environment whose egress policy
allow-lists `brand9signs.com` (e.g. wherever the `b9-*` / Mallard / Pulse
skills normally execute), or add `brand9signs.com` to this environment's
egress allow-list if this repo is meant to be the permanent home for the
scheduled job.
