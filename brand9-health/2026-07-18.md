# Brand 9 Site Health Report — 2026-07-18

## Overall status: 🔴 RED (audit blocked — network access denied)

This session's outbound egress proxy denied all connections to `brand9signs.com`
(CONNECT → 403, policy denial, confirmed via `/__agentproxy/status` and a direct
`curl` test). WebFetch failed identically on every URL attempted. As a result,
**none of the 6 scheduled checks could be executed** — this is not a site-health
finding, it is a tooling/access failure that needs to be fixed before this
routine can produce a real report.

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⛔ Not tested — WebFetch returned HTTP 403 from the egress proxy before reaching the site |
| 2 | Marketing/Branding category page (16-tile nav + 8 product cards + Motion Films block) | ⛔ Not tested — same proxy block |
| 3 | 5 random product pages: HTTP 200 + image + price | ⛔ Not tested — same proxy block |
| 4 | Yoast SEO meta on Motion Films product page | ⛔ Not tested — same proxy block |
| 5 | OG image renders for homepage | ⛔ Not tested — same proxy block |
| 6 | New posts/products in last 24h | ⛔ Not tested — same proxy block |

### What was attempted
- `WebFetch` on `https://brand9signs.com` → `HTTP 403 Forbidden` (no body)
- `WebFetch` on `https://brand9signs.com/product-category/marketing-branding/` → `HTTP 403 Forbidden` (no body)
- Direct `curl` to `https://brand9signs.com/` → `CONNECT tunnel failed, response 403`
- Proxy status endpoint (`/__agentproxy/status`) confirms: `"connect_rejected"`,
  `"gateway answered 403 to CONNECT (policy denial or upstream failure)"`,
  host `brand9signs.com:443`
- `WebSearch` for `brand9signs.com` succeeded (search snippets only) — confirms
  the domain resolves and is indexed, but gives no HTTP status, console output,
  or on-page verification, so it cannot substitute for the audit.

### Recommendation
The egress allowlist for this environment does not currently permit
`brand9signs.com`. Someone with access to the environment/network policy
settings needs to add it to the allowed destinations before this scheduled
check can run for real. Until then, this daily task will keep producing a
RED "blocked" report rather than an actual site-health signal.
