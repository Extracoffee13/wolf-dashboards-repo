# Brand 9 Site Health Check — 2026-08-15

## Overall status: 🔴 RED — check could not run

None of the 6 scheduled checks could be executed. This is a tooling/environment failure, not a
site-health finding — **do not read this as "the site is down."**

## Why

1. **No network path to brand9signs.com from this session.** `WebFetch` returned
   `EGRESS_BLOCKED — Access to brand9signs.com is blocked by the network egress proxy`, and a
   direct `curl` from the sandbox timed out (exit 56 / HTTP 000) on both the homepage and
   `/product-category/marketing-branding/`. This environment's egress proxy does not have
   brand9signs.com allowlisted, so `web_fetch`/`WebSearch`-based checks are structurally unable
   to reach the live site from here, independent of whether the site itself is healthy.
2. **The check spec doesn't match the known site architecture.** Per this repo's own operating
   notes (`b9-site-deploy`, `b9-verify-and-index` skills), brand9signs.com was rebuilt as a
   **static HTML site on DreamHost** (folder-per-slug pages like
   `/sign-company-orange-park-fl/`, `/jacksonville-sign-company/`) — it explicitly is **not**
   WordPress/WooCommerce anymore, and the WordPress/Yoast workflow is documented as stale/dead.
   A live search turned up sign-fabrication/installation content (Brand 9 Signs, Orange Park FL,
   est. 1986), consistent with that — not an e-commerce catalog. This scheduled task's checklist
   (`/product-category/marketing-branding/`, 16-tile nav + 8 product cards, per-product Yoast
   focus keyphrase/meta description/SEO title, per-product pricing) describes a WooCommerce/Yoast
   storefront that does not appear to exist on the current site. Even with network access
   restored, several of these checks may not be checkable as written.
3. Separately, this repo's own notes record that brand9signs.com sits behind an **sgcaptcha
   bot-shield** that returns a CAPTCHA redirect (HTTP 202, `/.well-known/sgcaptcha/`) to any
   cloud-side `curl`/fetch — so even with egress allowed, a plain `web_fetch` check from a
   cloud session would silently return captcha-page content, not real page content, unless
   fetched through an authenticated browser session. This session has no browser/Chrome tool
   available to fall back to.

## Checks attempted (all blocked)

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed | Not run — no egress |
| 2 | `/product-category/marketing-branding/` nav+cards+Motion Films block | Not run — no egress; also this URL path/structure is not corroborated by known site architecture |
| 3 | 5 random product pages: 200 + image + price | Not run — no egress; "product pages" with pricing not corroborated by known site architecture |
| 4 | Yoast SEO meta on Motion Films product page | Not run — no egress; site is documented as non-WordPress, so Yoast may not apply |
| 5 | OG image renders for homepage URL | Not run — no egress |
| 6 | New posts/products in last 24h | Not run — no egress |

## Recommendation

- Fix the egress allowlist for this session/environment (or run this check from a session with
  Claude-in-Chrome / an authenticated browser tool, per the `b9-verify-and-index` skill) before
  the next scheduled run.
- Reconcile this check's spec against the actual live site structure — confirm with a human
  whether brand9signs.com still has a WooCommerce catalog anywhere, or whether steps 2–4 should
  be rewritten around the real static-page inventory (service/location pages, answer pages).
