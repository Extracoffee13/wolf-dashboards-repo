# Brand 9 Signs — Site Health Check — 2026-08-12

## Overall status: 🔴 RED

This run could not execute the health check as designed. Two blocking issues, not a live-site outage — see below.

## Blocking issue 1 — no network path to brand9signs.com from this session
Every `WebFetch` call to `brand9signs.com` (homepage, `/product-category/marketing-branding/`, `/robots.txt`)
failed immediately with `EGRESS_BLOCKED — Access to brand9signs.com is blocked by the network egress proxy`.
This is a harder failure than the known sgcaptcha bot-shield (which normally returns a CAPTCHA redirect page,
not an outright block) — this session's outbound proxy is not letting requests to the domain leave at all.
None of steps 1, 2, 3, 5 (homepage load, category page layout, 5 product pages, OG image) could be performed.
No console-error, load-time, or image-render data is available this run.

## Blocking issue 2 — the check script assumes the wrong site architecture
Cross-checked against this repo's own internal docs (`b9-site-deploy`, `b9-verify-and-index` skills, verified
2026-08-08/10) and corroborated by `WebSearch`: **brand9signs.com is a static HTML build on DreamHost
(~357 pages, folder-per-slug), not WordPress, and has no WooCommerce/product catalog or Yoast SEO plugin.**
The real site is a signage/branding agency site with root-level answer pages
(`/sign-company-orange-park-fl/`, `/jacksonville-sign-company/`, `/marketing-services/`, `/portfolio/`,
`/about-us-brand-9/`), not a WooCommerce store. `WebSearch` found zero evidence a
`/product-category/marketing-branding/` URL, WooCommerce product pages, or a Yoast-managed "Motion Films
product page" exist on the live site. As specified, checks 2, 3, and 4 of this task (16-tile nav + 8 product
cards on a category page, price on 5 random product pages, Yoast focus keyphrase/meta description/SEO title)
do not map onto the current site and cannot be meaningfully answered either way — not "pass," not "fail,"
inapplicable to the actual build.

## What could be indirectly confirmed (via WebSearch only, not a live fetch)
- Domain resolves and is indexed; business identity (Orange Park, FL; est. 1986; homebuilder/military
  signage; motion graphics/brand agency services) matches known-good state.
- Motion/film-related content exists as an **agency service offering** ("narrative brand cinema," "motion
  graphics," hero reels/launch films under `/marketing-services/`), not as a WooCommerce product line — so
  "Motion Films product page" (check 4) likely doesn't exist in the form assumed.
- No visibility into: console errors, actual page load times, image rendering, OG image validity, or new
  posts/products in the last 24h. All require either a working egress path or the authenticated-browser
  verification path (Claude-in-Chrome / Cowork) documented in `b9-verify-and-index`, neither of which this
  CLI session has.

## Recommendation
1. **Verification path**: this scheduled check should run through the authenticated-browser path (Cowork /
   Claude-in-Chrome with the sgcaptcha cookie), per `b9-verify-and-index` — not a headless CLI session with
   plain `WebFetch`, which is now fully egress-blocked to this domain on top of the pre-existing captcha issue.
2. **Task drift**: the health-check script (steps 2–4) should be rewritten against the actual static-site
   structure (root-level answer pages, no WooCommerce, no Yoast) instead of the legacy WordPress/WooCommerce
   assumptions — likely inherited from before the site's rebuild.
3. Until both are fixed, this daily check cannot certify real health/RED/GREEN status — today's RED reflects
   "monitoring is broken," not a confirmed site outage.
