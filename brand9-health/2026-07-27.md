# Brand 9 Signs — Site Health Report
**Date:** 2026-07-27
**Overall status:** 🔴 RED — audit could not run (tooling/egress blocker, not a confirmed site outage)

## Summary

Today's scheduled health check could not execute. Every outbound request to
`brand9signs.com` (and `www.brand9signs.com`) was rejected at the session's
egress proxy with `403` before reaching the site at all — this is an
organization network-policy denial in this Claude Code environment, not a
response from the Brand 9 server. Per the proxy's own diagnostic guidance,
blocked hosts should be reported rather than retried or routed around, so
none of the 6 checks below could be completed as designed.

**This is not evidence the site is down.** `WebSearch` independently found
brand9signs.com pages indexed and reachable (see Evidence), which means the
site is being crawled/served to at least some traffic. The RED status here
reflects that **this automated check has no visibility into the site today**,
which is itself the actionable finding — not a claim that the site is broken.

## Diagnostics performed

- `WebFetch https://brand9signs.com` → `403 Forbidden` (no response body)
- `WebFetch https://www.brand9signs.com` → `403 Forbidden`
- `WebFetch https://brand9signs.com/product-category/marketing-branding/` → `403 Forbidden`
- `WebFetch https://brand9signs.com/sign-company-orange-park-fl/` → `403 Forbidden`
- Direct `curl` (bypassing the WebFetch tool, browser User-Agent) → `curl: (56) CONNECT tunnel failed, response 403`
- Agent-proxy status check → proxy itself healthy, no relay failures logged; the `403` originates from the egress policy layer, confirmed against `/root/.ccr/README.md`: *"403 / 407 from the proxy: The destination host is not allowed by your organization's egress policy for this session."*

## Checklist status (all blocked)

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⛔ Not testable — blocked at proxy |
| 2 | `/product-category/marketing-branding/` — 16-tile nav + 8 product cards + Motion Films block | ⛔ Not testable — blocked at proxy |
| 3 | 5 random product pages — HTTP 200, image, price | ⛔ Not testable — blocked at proxy |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | ⛔ Not testable — blocked at proxy |
| 5 | OG image for homepage renders | ⛔ Not testable — blocked at proxy |
| 6 | New posts/products in last 24h | ⛔ Not testable — blocked at proxy |

## Evidence the site itself is likely up

`WebSearch site:brand9signs.com` returned live, indexed URLs, including:
- The Evolution of Brand 9: From Banners to Branding — https://brand9signs.com/the-evolution-of-brand-9-signs/
- Sign Company Orange Park FL — https://brand9signs.com/sign-company-orange-park-fl/
- Jacksonville Sign Company — https://brand9signs.com/jacksonville-sign-company/

This is search-index evidence only (crawled at some prior time), not a live
fetch — it does not confirm current uptime, embed rendering, Yoast field
state, or today's content.

## Recommended action

Add `brand9signs.com` (and `www.brand9signs.com`) to this session's/environment's
egress allowlist so future scheduled runs of this health check can actually
reach the site. Until that's done, this daily audit will keep reporting RED
for "no visibility," which will mask real site issues underneath it. Consider
running this check from an environment that isn't proxy-restricted, or
explicitly allowlisting the domain, so tomorrow's run can produce a real
signal.
