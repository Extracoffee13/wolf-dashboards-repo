# Brand 9 Site Health Report — 2026-07-20

## Overall status: ⚫ BLOCKED — check could not run (not a confirmed site issue)

**This is not a RED for brand9signs.com.** The health checks in this report could
not be executed at all: outbound web access from this session's environment is
being denied at the network/proxy layer for *every* destination, not just
brand9signs.com. Treat the site's actual status as **unknown** until the check
can be re-run from an environment with working egress.

## What happened

- `WebFetch https://brand9signs.com` → HTTP 403
- `WebFetch https://brand9signs.com/product-category/marketing-branding/` → HTTP 403
- `WebFetch https://brand9signs.com/product/military-base-signs/` → HTTP 403
- Control checks to rule out a site-specific block:
  - `WebFetch https://example.com` → HTTP 403
  - `WebFetch https://www.google.com` → HTTP 403
- Direct `curl` to the egress proxy confirmed the cause:
  ```
  curl: (56) CONNECT tunnel failed, response 403
  ```
- Proxy status endpoint (`$HTTPS_PROXY/__agentproxy/status`) recorded the failures explicitly:
  ```json
  {
    "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "brand9signs.com:443"
  }
  ```
  The identical rejection for `example.com:443` confirms this is a session-wide
  egress policy denial, not something specific to brand9signs.com.
- `WebSearch site:brand9signs.com` **did** work (search is served differently than
  fetch) and confirmed the domain is indexed and serving pages under its normal
  titles (homepage, product pages, blog posts) as of the last crawl — this is
  weak positive evidence the site itself is up, but it is not a substitute for
  a live fetch and cannot verify today's console errors, embeds, Yoast fields,
  or OG image.

## Checklist status (all unverifiable this run)

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⚫ Not checked — fetch blocked |
| 2 | Marketing/Branding category page: 16-tile nav + 8 product cards + Motion Films block | ⚫ Not checked — fetch blocked |
| 3 | 5 random product pages: HTTP 200 + image + price | ⚫ Not checked — fetch blocked |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase/description/title, no placeholder leakage) | ⚫ Not checked — fetch blocked |
| 5 | OG image for homepage renders | ⚫ Not checked — fetch blocked |
| 6 | New posts/products in last 24h | ⚫ Not checked — fetch blocked (WebSearch shows only previously-indexed content, can't confirm recency) |

## Recommendation

Re-run this check from a session/environment whose egress policy allows
`brand9signs.com` (and general web access). If this recurs on the next
scheduled run, escalate to whoever manages this environment's network policy
rather than continuing to retry — per the proxy's own guidance, 403 policy
denials should be reported, not routed around.
