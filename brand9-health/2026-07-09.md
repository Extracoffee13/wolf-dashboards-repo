# Brand 9 Signs — Site Health Report
**Date:** 2026-07-09
**Overall status:** 🔴 RED — audit could not be executed (network egress blocked)

## Summary

This session's outbound network policy denies access to `brand9signs.com` outright: every `WebFetch` call to the domain (homepage, the Marketing/Branding category page, and a sample product page) failed at the CONNECT-tunnel stage with `403 Forbidden` from the egress proxy itself — not from the target site. Per the proxy's own guidance, a 403/407 at that layer means the destination host is not on this session's allowlist, and it should be reported rather than retried or routed around. As a result, none of the six requested checks could be performed directly. `WebSearch` was used as a partial fallback, but it cannot substitute for the live checks the task requires (HTTP status codes, console errors, image/price rendering, Yoast meta fields, OG image rendering).

**This report is a "monitoring failed" alert, not a clean bill of health.** Nothing below should be read as confirmation that the site is healthy.

## Check-by-check

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⛔ Not verifiable — WebFetch to `https://brand9signs.com` returned `403` at the network layer |
| 2 | `/product-category/marketing-branding/` shows 16-tile nav + 8 product cards + Motion Films block | ⛔ Not verifiable — same network block. WebSearch confirms the page exists and is indexed, but cannot confirm tile/card counts or the Motion Films block |
| 3 | 5 random product pages: HTTP 200 + image + price | ⛔ Not verifiable — sampled `/product/military-base-signs/` via WebFetch, same 403 network block |
| 4 | Yoast SEO on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | ⛔ Not verifiable directly. Notably, WebSearch turned up **no evidence of a dedicated "Motion Films" product page** on brand9signs.com at all — only general references to motion graphics as a service line. Worth confirming the page still exists and is titled/slugged as expected once fetch access is restored |
| 5 | OG image for homepage renders | ⛔ Not verifiable — requires direct fetch |
| 6 | New posts/products in last 24h | ⛔ Not verifiable via search; no reliable way to scope WebSearch results to a 24h window |

## Root cause

This is an environment/tooling issue, not a finding about the website itself: the session's network egress policy does not currently allow reaching `brand9signs.com`. `curl` through the configured proxy confirms the block occurs at the CONNECT stage (`403`) before any request reaches the site.

## Recommendation

Fix the egress allowlist for the environment/schedule running this routine so `brand9signs.com` (and any subdomains/CDN hosts it depends on, e.g. WordPress media/CDN) are reachable, then re-run this check. Until then, this daily job cannot fulfill its purpose and should be treated as failing, not passing.
