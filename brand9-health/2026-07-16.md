# Brand 9 Signs — Daily Site Health Check
**Date:** 2026-07-16
**Target:** https://brand9signs.com
**Overall status:** 🔴 RED — check could not be executed (environment access blocker, not a confirmed site defect)

## Summary

This run could not perform live verification of brand9signs.com. Direct
outbound network access to `brand9signs.com:443` is denied by this session's
egress policy at the proxy level:

```
kind: connect_rejected
detail: gateway answered 403 to CONNECT (policy denial or upstream failure)
host: brand9signs.com:443
```

Both `WebFetch` and a direct `curl` from the sandbox failed for this reason
(curl exit 56 / connection reset on CONNECT, WebFetch reporting HTTP 403).
Per the proxy's own guidance, this class of failure should be reported, not
retried or routed around — so no further fetch attempts were made against
the domain.

`WebSearch` (which queries a search index rather than fetching the live
page) does work and confirms the site and its URL structure are indexed and
resolvable in principle, but it cannot substitute for the checks this
routine requires (console errors, embed rendering, HTTP status codes on
specific product pages, Yoast meta field contents, OG image rendering, or
page load time). Treat everything below as "last known indexed state," not
a live health verdict.

## Checklist status

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⚪ NOT VERIFIED — live fetch blocked |
| 2 | Marketing/Branding category page: 16-tile nav + 8 product cards + Motion Films block | ⚪ NOT VERIFIED — live fetch blocked |
| 3 | 5 random product pages: HTTP 200, image, price | ⚪ NOT VERIFIED — live fetch blocked |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | ⚪ NOT VERIFIED — live fetch blocked |
| 5 | OG image for homepage renders | ⚪ NOT VERIFIED — live fetch blocked |
| 6 | New posts/products published in last 24h | ⚪ NOT VERIFIED — no live/authenticated feed access available |

## What WebSearch did surface (indexed, not live-verified)

- Homepage indexed as "Brand 9 Signs | Homebuilder, Military & Development Signage."
- `/product-category/marketing-branding/` indexed as "Marketing & Branding Archives | Brand 9 Signs," with individual products (Corporate Wall Branding, Home Builder Marketing, Corporate Branding Signage, Monument Signs, Custom Restaurant Signs, Dimensional Sign Letters) appearing under it in search results.
- No indexed evidence of a distinct "Motion Films" product/feature page was found via search — this specifically needs live verification once access is restored, since it's one of the named checks (homepage embed + category feature block).

## Root cause

This is an **environment/access issue**, not a confirmed site outage. The
egress policy for this session does not allow direct HTTPS connections to
`brand9signs.com`. This is the same failure mode called out explicitly in
the proxy's troubleshooting guide as "destination host is not allowed by
your organization's egress policy for this session."

## Recommended action

- Allowlist `brand9signs.com` (and its WP asset host, if different) in the
  egress policy for sessions that run this scheduled routine, or run this
  check from an environment/tool that has direct network access to the
  site (e.g. an MCP browser tool with real network access, or a monitoring
  service outside this sandbox).
- Until that's resolved, this daily check cannot produce a real health
  verdict — treat its output as "did not run" rather than "site is healthy."
