# Brand 9 Site Health Report — 2026-08-21

## Overall status: 🔴 RED — audit could not be completed

The 6-step health check could not be run as specified. Direct HTTPS access to
`brand9signs.com` (via `WebFetch` and via `curl`) is **blocked by this
environment's egress proxy** with a policy denial:

```
gateway answered 403 to CONNECT (policy denial or upstream failure)
host: brand9signs.com:443
```

This is not a transient network error — the proxy status endpoint
(`$HTTPS_PROXY/__agentproxy/status`) confirms `brand9signs.com` is being
rejected at the CONNECT-tunnel level by gateway policy, for both `WebFetch`
and raw `curl`. No console-error inspection, HTTP status codes, image
rendering checks, Yoast field inspection, or OG-image rendering could be
performed this run. `WebSearch` (which goes through a separate search
backend, not a direct fetch) still worked and was used to gather best-effort,
indirect signal below — but it is **not a substitute** for the requested
checks and cannot confirm 200s, console errors, or live meta values.

**Action needed:** this session's network egress policy needs
`brand9signs.com` allow-listed (or the check needs to run from an
environment that has it), or this daily check cannot do its job.

---

## Best-effort findings (via WebSearch only — unverified, indexed/cached data)

1. **Homepage / motion film embed** — not verifiable. Could not fetch the
   page or inspect console/embed rendering.

2. **`/product-category/marketing-branding/`** — the URL is indexed by
   Google under the title *"Marketing & Branding Archives | Brand 9 Signs"*,
   consistent with a live WooCommerce/WordPress category-archive page. Tile
   row / 8 product cards / Motion Films block underneath could not be
   verified.

3. **5 random product pages** — could not do live 200/image/price checks.
   Indexed product URLs do exist, e.g.:
   - `/product/modern-street-signs/`
   - `/product/custom-3d-wall-sign-customizable/`
   - `/product/commercial-lighted-signs/`
   Status codes, images, and prices on these were not verified this run.

4. **Yoast SEO meta on the Motion Films product page** — not verifiable.
   Could not locate/confirm a distinct "Motion Films" product page via
   search, and no field-level Yoast data (focus keyphrase, meta description,
   SEO title, placeholder leakage) can be checked without a live fetch.

5. **OG image for homepage** — not verifiable, no live fetch possible.

6. **New posts/products in last 24h** — not reliably determinable from a
   search index (indexing lag). No August 2026 content surfaced in search;
   inconclusive either way.

---

## Notable discrepancy worth flagging

The repo's `b9-site-deploy` skill states brand9signs.com was **rebuilt as a
static HTML site (not WordPress)** and that `/wp-json/` now 404s on a dead
legacy backend — it explicitly warns against using Yoast/WordPress workflows
for B9 as stale. However, this task's checklist (Yoast SEO fields,
WooCommerce-style `/product/` and `/product-category/` URLs) assumes a live
WordPress+WooCommerce+Yoast stack, and Google's index currently shows those
same WordPress-style URLs/titles as live. Either:
- the static rebuild coexists with WordPress on some paths,
- the rebuild hasn't fully replaced the WooCommerce catalog yet, or
- this health-check's premise (and/or the deploy skill) is out of date.

This should be reconciled independently of today's network-access blocker —
it affects whether the Yoast-specific check in this routine is even the
right thing to be checking going forward.

---

## Summary

| Check | Result |
|---|---|
| Homepage loads, console errors, motion film embed | ⛔ Unable to verify (egress blocked) |
| Marketing/Branding category page layout | ⛔ Unable to verify (egress blocked) |
| 5 random product pages (200 + image + price) | ⛔ Unable to verify (egress blocked) |
| Yoast meta on Motion Films product page | ⛔ Unable to verify (egress blocked) |
| Homepage OG image | ⛔ Unable to verify (egress blocked) |
| New posts/products in last 24h | ⛔ Inconclusive (search index only) |

**Headline finding:** this environment cannot reach `brand9signs.com` at
all right now — the daily health check is currently non-functional here,
independent of the site's actual health.
