# Brand 9 Site Health Report — 2026-07-21

## Overall Status: 🟡 YELLOW

**Headline finding:** Automated direct fetch (WebFetch) was blocked with HTTP 403 on *every* URL tested on brand9signs.com today, including `/robots.txt`. This pattern — a blanket 403 across unrelated paths, combined with WebSearch showing the site fully indexed and serving fresh content to search crawlers — points to a bot/WAF protection rule blocking this tool's requests, not an actual site outage. Direct sub-checks (console errors, embed rendering, tile/card counts, HTTP status per product, Yoast fields, OG image render) could **not** be independently verified this cycle. Flagging YELLOW rather than RED because there is no positive evidence of real breakage, but this is a monitoring gap that needs a fix (see Recommendations).

---

## 1. Homepage (https://brand9signs.com)
- **Status:** ⚠️ Unverified — WebFetch returned `403 Forbidden`.
- Could not confirm console errors or motion film embed rendering directly.
- WebSearch confirms the homepage is live and indexed: "Homebuilder Signage Florida | Sign Company | Brand 9 Signs".

## 2. Marketing/Branding category page (/product-category/marketing-branding/)
- **Status:** ⚠️ Unverified — WebFetch returned `403 Forbidden`.
- Could not confirm the 16-tile nav row, 8 product cards, or Motion Films feature block layout.
- WebSearch confirms the page is live and indexed ("Marketing & Branding Archives | Brand 9 Signs") and surfaces related product pages (Graphics, Marketing Strategy, Corporate Wall Branding, Corporate Branding Signage).

## 3. Random product pages (5x, HTTP 200 + image + price)
- **Status:** ⚠️ Unverified — WebFetch returned `403 Forbidden` on the one product URL sampled (`/product/graphics/`); did not attempt the remaining 4 given the consistent block.
- No HTTP status, image, or price could be confirmed directly.

## 4. Yoast SEO meta — Motion Films product page
- **Status:** ⚠️ Unverified / **Not located.**
- Could not find a distinct "Motion Films" *product* page via WebSearch (only general references to "motion graphics" as a service, and the category-level "Motion Films" feature block referenced in the task). Worth a manual check on whether this page exists at the expected URL and, if so, whether it's indexed yet — a not-yet-indexed page combined with today's fetch block means Yoast fields (focus keyphrase, meta description, SEO title) could not be checked for "yoast-placeholder" leakage this cycle.

## 5. OG image — homepage
- **Status:** ⚠️ Unverified — could not fetch homepage HTML to extract the `og:image` tag, and could not fetch any candidate image URL directly (all blocked).

## 6. New posts/products in last 24h
- **Status:** No new posts/products confirmed via WebSearch. Most recent content surfaced was dated March 30, 2026 (older). No indication of new publishes in the last 24 hours, but this is a low-confidence read given search-index lag.

---

## Recommendations
1. **Investigate the WebFetch 403 block** — test whether it's an IP/user-agent block from the site's WAF (Cloudflare/Wordfence/etc.) against this automation's egress, and consider allowlisting or switching to an authenticated/alternate fetch path for future daily checks.
2. **Manually verify** (from a normal browser) items 1–5 above at least once to confirm no real regression is hiding behind the fetch block.
3. **Confirm the "Motion Films" product page exists** at its intended URL and is indexed; if newly created, run the Yoast field check once WebFetch access is restored.
