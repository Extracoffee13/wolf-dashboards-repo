# Brand 9 Site Health Check — 2026-07-25

## Overall status: 🔴 RED (check could not run — network policy block, not a site defect)

## What happened

This session's outbound egress policy denied all HTTPS connections to
`brand9signs.com`. Every attempt to reach the domain — via `WebFetch` and via
a direct `curl` through the session's agent proxy — failed at the CONNECT
stage with an explicit policy denial, before any request reached the site:

```
$ curl -D - https://brand9signs.com/
HTTP/1.1 403 Forbidden
curl: (56) CONNECT tunnel failed, response 403
```

The proxy status endpoint (`$HTTPS_PROXY/__agentproxy/status`) confirmed this
is a policy-level rejection, not a site-side error:

```json
"recentRelayFailures": [
  {
    "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "brand9signs.com:443"
  }
]
```

Per the proxy's own guidance, this class of failure ("403 from the proxy")
means the destination host is not on this session's egress allowlist, and
should be reported rather than retried or routed around. No further retries
were attempted.

## Checks attempted (all blocked, 0/6 completed)

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, motion film embed renders | ❌ Blocked — could not connect |
| 2 | Marketing/Branding category page structure (16-tile nav + 8 product cards + Motion Films block) | ❌ Blocked — could not connect |
| 3 | 5 random product pages (HTTP 200, image, price) | ❌ Blocked — could not connect |
| 4 | Yoast SEO meta on Motion Films product page | ❌ Blocked — could not connect |
| 5 | OG image on homepage | ❌ Blocked — could not connect |
| 6 | New posts/products in last 24h | ❌ Blocked — could not connect |

## Secondary signal (not a substitute for a real check)

A `WebSearch` for `site:brand9signs.com` did return indexed pages (e.g.
`/the-evolution-of-brand-9-signs/`, `/sign-company-orange-park-fl/`,
`/jacksonville-sign-company/`), which implies the site is reachable and
indexed from the open internet in general. This says nothing about today's
actual homepage/embed/Yoast state — it's only evidence the domain isn't
globally down.

## Why this is marked RED

The health check's job is to catch drift before it matters. A silent
"couldn't run" would be worse than a false GREEN — none of the 6 requested
checks executed, so today's report carries no information about the actual
site state. Treat this as an unmonitored day, not a clean one.

## Recommended action

Add `brand9signs.com` to this environment's/session's egress allowlist so
future scheduled runs of this check can actually reach the site. Until that's
done, this daily check cannot produce a real signal — it will keep failing
in exactly this way regardless of the site's true health.
