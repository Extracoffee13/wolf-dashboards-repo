# Brand 9 Site Health Report — 2026-08-25

## Overall status: 🟡 YELLOW — audit incomplete (environment network restriction, not a confirmed site failure)

This session's execution environment blocks outbound `WebFetch` to `brand9signs.com` at the
network egress proxy level (`EGRESS_BLOCKED` on every attempt — homepage, category page, and
sitemap.xml all rejected before any HTTP response was received). This is a restriction of the
session's network policy, not a signal about the live site. Because of this, the six checks below
could **not** be run as live in-browser/HTTP checks per the runbook. `WebSearch` was used as a
partial substitute where possible, but it only surfaces indexed snippets/titles from Google's
crawl cache, not live console errors, HTTP status codes, rendered embeds, or Yoast meta fields.

**No RED or GREEN verdict is being asserted on live site health today** — asserting either would
be a fabrication. Flagging YELLOW so this gap is visible and re-run access can be fixed.

## Checks

### 1. Homepage load / console errors / motion film embed
**NOT VERIFIED.** WebFetch to `https://brand9signs.com` returned `EGRESS_BLOCKED` before any
response. No console-error or embed-render data available this run.

### 2. `/product-category/marketing-branding/` — 16-tile nav row + 8 product cards + Motion Films block
**NOT VERIFIED.** Same egress block. WebSearch confirms the page and adjacent product pages
(e.g. `/product/marketing-strategy/`, `/product/corporate-wall-branding/`) are indexed and exist,
but layout/tile-count/card-count cannot be confirmed without a live render.

### 3. 5 random product pages — HTTP 200 + image + price
**NOT VERIFIED.** Could not issue HTTP checks against the domain this run.

### 4. Yoast SEO meta on the Motion Films product page (keyphrase, meta description, SEO title, no `yoast-placeholder` leakage)
**NOT VERIFIED.** Could not load the page source or rendered `<head>` to inspect Yoast fields.

### 5. OG image render for homepage URL
**NOT VERIFIED.** Could not fetch or render the OG image URL.

### 6. New posts/products in the last 24h
**INCONCLUSIVE.** A WebSearch sweep (`brand9signs.com new blog OR product August 2026`) surfaced
no obviously new content dated within the last 24h, but search-index freshness lag means this is
not a reliable signal for same-day publishing. Existing indexed pages seen: `/blog-news/`,
`/brand-lab-ai/`, `/case-studies/`, `/store-effective-signage-branding/`, `/product-tag/signs/`,
`/product-category/school-branding/` — none showed an obvious new-today timestamp in search
snippets.

## Root cause / action needed
The blocker is this session's network egress policy, which denies `brand9signs.com` outright
(confirmed via three separate `WebFetch` calls, all `EGRESS_BLOCKED`). To restore this daily
check, either:
- run it from an environment/session whose egress policy allows `brand9signs.com`, or
- run it via the browser-based collector described in the `b9-site-health-sentinel` skill
  (Hermes/CDP-driven Chrome), which is the documented workaround for this site's bot-challenge
  and would also sidestep a plain-fetch egress block if that environment's policy differs.

## Next run
Retry with a session/environment that has egress access to `brand9signs.com`. If the block
persists, escalate to the runbook owner — a fully blind daily check is a false sense of coverage.
