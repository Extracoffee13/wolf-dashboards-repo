# Brand 9 Site Health Report — 2026-09-02

**Overall status: 🔴 RED — audit could not be executed as specified**

## Blocker (read this first)

This session's `WebFetch` tool returned `EGRESS_BLOCKED` for `brand9signs.com` — and, on a
control test, for `example.com` as well. This confirms it is not a brand9signs.com-specific
block (e.g. the known sgcaptcha bot-shield documented in the `b9-verify-and-index` skill):
**this environment's network egress proxy blocks WebFetch to any domain outright.** No page
content, HTTP status codes, console errors, images, or meta tags could be retrieved directly.

This session also has no Chrome/computer-use/Hermes browser access (`ListAgents` returned no
reachable agents), so the documented workaround — verify through an authenticated real browser
that holds the sgcaptcha cookie — is not available here either.

The only working tool was `WebSearch`, which returns indexed snippets, not live page fetches.
It cannot check HTTP status codes, render embeds, count DOM elements, inspect Yoast SEO fields,
or confirm image/OG rendering. Findings below are therefore **indirect and incomplete**, not a
substitute for the requested 6-point audit.

## What WebSearch could establish (indirect signal only)

1. **Homepage load / motion film embed** — NOT VERIFIABLE (no console-error or embed-render
   visibility possible via search).
2. **Marketing/Branding category page** (`/product-category/marketing-branding/`) — page is
   indexed and search results surface individual products under it (e.g. `/product/marketing-strategy/`,
   `/product/corporate-wall-branding/`), consistent with the category existing and having products.
   Tile-row / card-count / Motion Films feature-block layout — NOT VERIFIABLE.
3. **5 random product pages: HTTP 200 + image + price** — NOT VERIFIABLE (no status-code or
   DOM access from this session).
4. **Yoast SEO on the Motion Films product page** (keyphrase, meta description, SEO title,
   no `yoast-placeholder` leakage) — NOT VERIFIABLE. Could not even confirm the exact product
   URL from search alone.
5. **OG image render for homepage** — NOT VERIFIABLE.
6. **New posts/products in last 24h** — NOT VERIFIABLE (no way to sort/filter by publish date
   via WebSearch; nothing in search results indicated new content, but absence of evidence is
   not evidence of absence here).

## Recommendation

Run this check from a session with either (a) WebFetch egress allowed to brand9signs.com, or
(b) authenticated real-browser access (Claude-in-Chrome / Hermes), per the `b9-site-health-sentinel`
and `b9-verify-and-index` skills. This session (`wolf-dashboards-repo`, GitHub-scoped remote
execution environment) has neither. Marking RED because the audit's critical checks — HTTP
status, embed rendering, and Yoast meta integrity — are exactly the things that could not be
confirmed, so a false GREEN would be worse than flagging the gap.
