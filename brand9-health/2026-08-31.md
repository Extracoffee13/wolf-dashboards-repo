# Brand 9 Site Health Report — 2026-08-31

## Overall status: 🔴 RED (check could not run)

## Headline finding

**All six checks are blocked at the network layer — this is an infrastructure/access failure in the
executing session, not a finding about brand9signs.com itself.** Every outbound request to
`brand9signs.com` from this environment was rejected before reaching the site:

- Direct `curl` to `https://brand9signs.com` → `403 Forbidden` from the egress proxy (`CONNECT tunnel
  failed, response 403`).
- `WebFetch` to the same URL → `EGRESS_BLOCKED` error: *"Access to brand9signs.com is blocked by the
  network egress proxy."*
- The proxy's own status log confirms a policy-level denial, not a transient network blip:
  `gateway answered 403 to CONNECT (policy denial or upstream failure)`, host
  `brand9signs.com:443`, timestamped 2026-08-31T16:08:46Z.

No page was ever reached, so none of the six requested checks could be performed:

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | **NOT RUN** — egress blocked |
| 2 | `/product-category/marketing-branding/` layout (16-tile nav + 8 cards + Motion Films block) | **NOT RUN** — egress blocked |
| 3 | 5 random product pages: HTTP 200 + image + price | **NOT RUN** — egress blocked |
| 4 | Yoast SEO meta on Motion Films product page | **NOT RUN** — egress blocked |
| 5 | OG image for homepage renders | **NOT RUN** — egress blocked |
| 6 | New posts/products in last 24h | **NOT RUN** — egress blocked |

## Why this matters beyond today

This session's egress policy does not allowlist `brand9signs.com`. Separately, prior runs of this same
audit (per the `b9-site-health-sentinel` skill in this repo) have established that even when egress is
open, a plain `curl`/`WebFetch` request to this domain gets a bot-challenge interstitial (~181-byte
placeholder, HTTP 202) rather than the real page — so this check has a hard *and* a soft dependency on
running inside an authenticated real browser session (Hermes CDP / Claude-in-Chrome), which this session
also does not have available. Both blockers need to be resolved for this daily check to produce a real
result:

1. Add `brand9signs.com` to this environment's egress allowlist, **or** run the check from an
   environment that already has it (e.g. wherever Hermes/Claude-in-Chrome browser access lives).
2. Run the scan through a real authenticated browser tab, not a server-side HTTP client, per the
   `b9-site-health-sentinel` skill.

## What this report does NOT claim

This report makes no assertion, positive or negative, about the actual state of brand9signs.com today —
whether the homepage loads, whether Yoast metadata is populated, whether products return 200s, or
whether anything published in the last 24 hours. None of that was observable from this session. Treat
the site's real status as **unknown** until a run with working access completes.

## Recommended next step

Re-run this check from a session/environment with `brand9signs.com` egress access and a real browser
(Hermes CDP or Claude-in-Chrome), per `b9-site-health-sentinel` and `b9-verify-and-index`.
