# Brand 9 Signs — Daily Site Health Check
**Date:** 2026-07-03
**Target:** https://brand9signs.com
**Overall status:** 🔴 RED — health check could not run (egress blocked)

## Summary

This run could not reach `brand9signs.com` at all. Every outbound HTTPS attempt
(via `web_fetch` and via a direct `curl` sanity check) failed with `403` at the
network layer, and the agent proxy status endpoint confirms it: the egress
gateway itself rejected the CONNECT to `brand9signs.com:443` as a **policy
denial**, not a response from the site:

```
"kind": "connect_rejected",
"detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
"host": "brand9signs.com:443"
```

Per this environment's proxy guidance, policy-denied hosts should be reported,
not retried or routed around — so none of the 6 checks below could be executed
as designed. This is an infrastructure/access gap in the monitoring
environment, not a confirmed finding about the live site.

One signal is reassuring: a `WebSearch` for `brand9signs.com` returned the
homepage as an indexed, titled result ("Brand 9 Signs | Homebuilder, Military
& Development Signage"), which suggests the domain is resolving and served
recently as of Google's last crawl. That is not a substitute for the
first-party checks requested, though, and cannot confirm today's console
errors, embed health, pricing, or Yoast metadata.

## Check-by-check status

| # | Check | Result |
|---|-------|--------|
| 1 | Homepage loads, no console errors, motion film embed renders | ⚠️ **BLOCKED** — could not fetch |
| 2 | `/product-category/marketing-branding/` shows 16-tile nav + 8 product cards + Motion Films block | ⚠️ **BLOCKED** — could not fetch |
| 3 | 5 random product pages: HTTP 200 + image + price | ⚠️ **BLOCKED** — could not fetch (could not even enumerate product URLs via `/shop/` or `/sitemap.xml`) |
| 4 | Yoast SEO meta on Motion Films product page (keyphrase, meta description, SEO title, no placeholder leakage) | ⚠️ **BLOCKED** — could not fetch |
| 5 | OG image for homepage renders | ⚠️ **BLOCKED** — could not fetch |
| 6 | New posts/products in last 24h | ⚠️ **BLOCKED** — could not fetch |

## Root cause

- `web_fetch https://brand9signs.com` → `HTTP 403 Forbidden` (no body)
- `curl -A "<browser UA>" https://brand9signs.com/` → connection failed, `403`
  from the proxy CONNECT itself (confirmed via `/__agentproxy/status`)
- Recorded proxy failure: `connect_rejected` / `policy denial or upstream
  failure` for `brand9signs.com:443`, twice, at 2026-07-03T16:03:54Z

This reads as this session's network egress policy not allowlisting
`brand9signs.com`, rather than the site being down or blocking us with a WAF.

## Action needed

- Confirm `brand9signs.com` is allowlisted in this environment's egress
  policy for the scheduled health-check session, or run this check from an
  environment that permits outbound access to the domain.
- Re-run the full 6-point check once access is restored; treat today as a
  gap in monitoring coverage, not a clean bill of health.
